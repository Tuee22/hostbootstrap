# System Components

**Status**: Authoritative source
**Supersedes**: N/A
**Referenced by**: [README.md](README.md), [00-overview.md](00-overview.md),
[development_plan_standards.md](development_plan_standards.md)

> **Purpose**: Inventory the host-management components the repository implements, identify partial or
> definition-only surfaces honestly, and point each open contract to its owning phase.

## Status and Evidence Rule

Current phase status is reported only by the
[README phase table](README.md). This inventory contains no phase-status roll-up and
no mutable “current test count.” Dated validation evidence belongs in phase sprints.

`Implemented` below means the named module/surface exists and satisfies its contract. `Partial` means code
exists but the contract is not fully built, and the owning phase is `Active` and says which sprint carries the
rest. There is no `Definition-only` state: a surface with no consumer is a surface no phase should introduce
(§ A), so it is removed by rewriting the phase rather than tracked.

## hostbootstrap-core Module Surface

| Module | State | Purpose / open contract |
|--------|-------|-------------------------|
| `HostBootstrap.CLI` | Implemented static construction boundary | Fixed command entrypoint and identity-parametric `ProjectSpec cfg tcfg`; the static value carries no lifecycle scope, specification digest, plan, or configuration identity. Scope finalization yields one exact `FinalizedProjectSpec scope specDigest cfg` retaining the matching codec, typed service registry, plan builder, and project-owned forward-child projector. Production and Harness each retain one exact scope-indexed plan per invocation. Every real-project builder installs one projector; the bare-project path retains a fixed refusal. The projector remains private inside the finalized specification and feeds only the hidden exact planned-forward package. Neither the static builder nor its projector launches a process, opens a channel, accesses a child store, or creates recursive command authority |
| `HostBootstrap.ProjectPlan` | Implemented pure facade | Opaque `PlanDraft`, generative `ProjectPlan`, `PlannedStep`, `DerivedTopology`, plan-owned resources and edges, and public non-authorizing snapshots form the pure plan surface. Admission retains the exact scope, specification, configuration, canonical root, nodes, topology, stable bytes, resource relations, step identities, reverse policy, and declared reverse callbacks. Production and Harness retain that exact value through root interpretation and reverse projection. The finalized forward-child projector and `PlannedForwardHandoff` are implemented lower substrate: they prove exact topology, configuration, and child-plan correspondence without forging a child-local root. They have no runtime caller and grant no process, channel, store, journal, cursor, or effect authority. The active rooted coordinator work is owned by [the recursive-lifecycle-command phase](phase-17-recursive-lifecycle-command.md); concrete demo lineage remains with [the worked-demo phase](phase-24-worked-demo.md) |
| `HostBootstrap.ProjectPlan.Construct` | Implemented fresh, recovered, and projected-child construction boundary | `FinalizedProjectSpec`, non-empty draft projection, fresh rank-2 plan admission, fixed-identity Production reconstruction, and the private forward-child projection kernel are implemented. The kernel validates projected configuration through the exact finalized codec and exposes neither raw projector bytes nor a caller-polymorphic result. Child construction yields the exact projected `ProjectPlan`, `PlanDigestBinding`, and opaque `ChildPlanAuthority`; that authority proves only plan/configuration correspondence. It does not confer ProtectedStore, journal, cursor, command, signing, process, or backend-effect authority. The boundary remains a caller-free lower substrate for the planned rooted catalog and storeless frame executor |
| `HostBootstrap.Lifecycle.Plan` / `HostBootstrap.ProjectPlan.Handoff.Internal` | Implemented exact planned-forward and catalog-admitted forward packages; zero runtime callers | The hidden `PlannedForwardHandoff` producer consumes the exact parent plan, current frame, root-coordinator-resident lifecycle context, and finalized child projector. It owns only the lifecycle-context join and the sealed package: it borrows the root or nested context, checks the supplied, retained, and project frame evidence, and then delegates descriptor, context, configuration, and target-plan validation to the shared immediate-target kernel. The package forges no child-local `CanonicalProjectRoot`, contains no independently selectable executable or `SelfRef`, and performs no process, channel, journal, cursor, store, or effect action. The rooted catalog adopts the same kernel's evidence before any child effect. The same owner holds the storeless `CatalogForwardHandoff scope rootPlanId brokerGeneration catalogId parentFrame childPlanDigest childConfigId childFrame`, whose eight indices are nominal and whose only producer goes through the catalog's own edge fold. It rechecks the admitted child against the evidence the selected entry retains — target-plan current frame, validated-configuration endpoint, rendered plan digest against the binding's, configuration and payload digests against each other and against the canonical payload's own hash, and both routes at exactly one lift layer — then rebuilds the binding input from that evidence alone and forces all thirteen retained fields before either callback. It retains no lifecycle context, parent plan, or specification index, and its one eliminator exposes only the stripped route, binding input, and canonical payload under a fixed unit result |
| `HostBootstrap.ProjectPlan.Projection.Internal` | Implemented shared VLC-free projection kernel; two hidden callers | `withImmediateTargetKernel` is the one boundary that proves the unique immediate ancestry edge and joins the projected descriptor, canonically validated child configuration, target plan, digest binding, topology evidence, raw and stripped route, canonical payload, both digests, and invocation input. VM and VM-container ancestry remain exact, and the one permitted direct-container durable-source rebase is structurally constrained. It retains no `ValidatedLifecycleContext`, canonical root, protected store, or invocation authority, which is what lets the exact single-edge package and the recursive catalog share it without either borrowing the other's lifecycle evidence. It is reachable only from `HostBootstrap.ProjectPlan.Handoff.Internal` and `HostBootstrap.Lifecycle.RootedPlan` |
| `HostBootstrap.Lifecycle.RootedPlan` | Implemented root-owned recursive catalog and its durable manifest; one runtime caller | Hidden all-nominal `RootedPlanCatalog scope rootPlanId brokerGeneration catalogId` is its own entry carrier. Construction consumes the exact finalized specification, root invocation authority, root plan and current frame, and root-resident lifecycle context, rechecks root residency, frame evidence, the admitted context endpoint, and the authority's installed project and durable store identity, then recursively projects every declared child through the shared immediate-target kernel. Each admitted descent retains the existential exact target `ProjectPlan`, `PlanDigestBinding`, `CurrentFrame`, raw and stripped plan-owned route, canonical config bytes, config and payload digests, parent/child edge, and the parent frame's plan-owned projected node keys. Descent terminates at a frame with no declared edge and is bounded by the root topology's own frame count. Selection is available only through rank-2 folds on the catalog; there is no entry, row, or nested lifecycle-context route a caller can hold. The value grants no journal, cursor, session, grant, signing, process, or protected-store operation. The same owner renders the one bounded canonical manifest and strictly compares an observed one, and names no store, session, record, or compare-and-swap operation itself: every variable-width value is length-framed, every list counted, both an admitted-edge and a byte ceiling refuse rather than truncate, and digests stand in for any raw configuration payload. Its record identity is the root plan's installed project, stable profile, and broker epoch, encoded by the entry through the store's injective record-name encoding. One rank-2 edge fold additionally selects an admitted descent by exact parent and child frame — no entry naming the requested child is missing, more than one is a duplicate, and one reached from another parent is a sibling of the requested edge — and rechecks that entry against the parent level's own retained plan and current frame, so a parent frame that is not that level's current frame, a descent the parent plan does not declare with exactly the retained raw route, and projected node keys that are not the parent plan's own each refuse before the continuation; the fold discloses the parent level only as its own `CurrentFrame`. `HostBootstrap.Command.LifecycleEntry`, `HostBootstrap.ProjectPlan.Handoff.Internal`, and `HostBootstrap.Teardown.Internal` are the only importers; the last takes only the canonical child-configuration bytes and their digest from an admitted reverse edge. [The recursive-lifecycle-command phase](phase-17-recursive-lifecycle-command.md) owns root frame sessions and every later consumer |
| `HostBootstrap.ProjectPlan.Frame` | Implemented pure evidence boundary with cursor, authority, and reverse-projection consumers | Sprint 12.14's `withCurrentFrame` purely joins the exact plan to descriptive binary context and jointly generates hidden-constructor `CurrentFrame`, `ProjectFrame`, and `ValidatedContext` under one fresh frame index. It retains the semantic frame `Text` unchanged; Sprint 12.22 defines its canonical UTF-8 cursor-record binding, Sprint 12.23 consumes the exact `ProjectFrame` at cursor admission, Sprint 12.25 consumes the matching `ProjectFrame`/`ValidatedContext` at exact local `project up` authorization, and Sprint 12.26 consumes `CurrentFrame` with the exact `ProjectPlan` to index the pure reverse projection. Pure frame values still grant no journal, cursor, command, lease, or mutation authority |
| `HostBootstrap.ProjectPlan.Snapshot` | Implemented root snapshot and reverse-intent boundary | Stable snapshot projection, pure digest binding, non-authorizing bound evidence, origin-checked persistence/readback, fresh lease compare-and-swap, existing Production admission, recovered-profile refinement, and exact recovered-plan reconstruction are implemented. The private reverse-root intent engine records and resumes exact verb-tagged source and target coordinates under one root-owned liveness extent. Its hidden root entry reconstructs only the same plan identity and broker epoch. This surface belongs to the root coordinator: it never exports a child store opener, child cursor admission, raw intent producer, or standalone resume authority. The planned rooted runtime consumes it through fixed hidden callbacks |
| `HostBootstrap.Command` | Partial root coordinator and storeless-executor adoption | Parser/dispatch, test grammar, Production fresh/recovered root admission, Harness plan retention, rendering, snapshot binding, root-refined lifecycle-context admission, fixed root-Up interpretation, and exact current-frame teardown are implemented. Both root `project up` entries hold a finalized specification at their plan's own specification index — the recovered one relabelled by the digest-proven join rather than re-finalized — and the shared bound body refuses before any lifecycle effect unless that specification's digest is the bound plan snapshot's. The root entry alone owns `ProtectedStore`, invocation lease, acquisition journal, per-frame durable cursors, root command authorization, and the current local `Chain` call. It also admits the recursive catalog once the journal has revalidated the live lease, snapshot, and plan digest, compare-and-swaps that catalog's bounded canonical manifest, and strictly re-reads it: an exact retry and a compare-and-swap loser converge, while conflicting bytes or a refused descent projection refuse before the command reservation and before any effect. Declared descent still fails closed at the `Chain` dispatch. The target command path installs one root coordinator that owns the recursive plan catalog, every prepare/settle/receipt transition, and all child-process lifetimes; long-lived children receive only root-signed exact grants and execute as storeless `FrameExecutor`s with per-frame `ResourceCarrier`s. No child opens the root store or mints local durable command authority. [The authenticated-handoff phase](phase-13-authenticated-handoff-and-child-admission.md) owns the rooted wire and scope-first receiver; [the recursive-lifecycle-command phase](phase-17-recursive-lifecycle-command.md) owns catalog, coordinator, executor, process, forward, reverse, cleanup, and failure adoption. Effect-indexed selected-service dispatch remains with [the service-runtime phase](phase-22-service-runtime.md) |
| `HostBootstrap.Detached` | Implemented boundary | The closed boundary for spawning a child that outlives its launcher (§ HH, [the host-tools-and-substrate-detection phase](phase-3-host-tools-and-substrate-detection.md), landed 2026-08-03). Its assembled `CreateProcess` is private, and so are `DetachedLaunch`'s constructor and *every field accessor*, so the stdio disposition, descriptor inheritance, session, and console detachment are properties of the boundary rather than fields a call site fills in; the executable is an `AbsExe` and the working directory and output sink are absolute by construction. `withDetachedChild` is a rank-2 bracket over the launch, not the child's lifetime: acquire-and-spawn is total, and the body's exceptions propagate unchanged. Standard input is the host's null device (open, at EOF) and both output streams share one retained sink the launcher reads to quote a startup failure (§ CC). `ForgeDetachedLaunch.hs`/`RelabelDetachedLaunch.hs` pin the seals and `DetachedSpec` observes a real child through the boundary. Closed on the Apple Silicon lane 2026-08-03 at `10/10 passed`, where the host daemon reached readiness on all four bring-ups |
| `HostBootstrap.HostTool` | Implemented boundary | Closed tool enumeration and `AbsExe`; every governed host call resolves to an absolute path |
| `HostBootstrap.HostConfig` | Implemented | Resolved host configuration |
| `HostBootstrap.HostPrereqs` | Implemented floor | Haskell host prerequisites aligned with the pre-binary Python floor |
| `HostBootstrap.Substrate` | Implemented | Apple/Linux/Windows CPU/GPU substrate classification |
| `HostBootstrap.Ensure*` | Config-free reconcilers and exact direct-Colima boundary implemented | The nine config-free reconciler families and their owning completed gates remain unchanged. `HostBootstrap.Ensure.Colima` is a separate plan-owned adapter, not a registry reconciler. [The cluster-lifecycle, budgets, and cordoning phase](phase-16-cluster-lifecycle-and-cordoning.md) Sprints 16.17–16.26 close its private resolver, bounded runner/supervisor, descriptor-held `flock(2)` filesystem/origin/context transaction, and opaque acquisition backend; Sprint 16.20's Cabal-private scoped fixture seam drives strict resolver/adaptor integration without becoming public authority, and Sprint 16.29's private install kernel orders retained-Brew revalidation, bounded fixed invocation, and complete fresh rediscovery. Sprints 16.27–16.37 adopt that boundary for exact `PreparedGate`-bound profile preparation, acquisition, provider/wall settlement, live Docker, and separately journaled conditional force-destroy cleanup. Production recursive and demo adoption remain downstream; completed Incus and WSL foundations retain their existing status |
| `HostBootstrap.Cluster.Cordon.Foundation` | Implemented exposed lower boundary | [The canonical-quantities-and-reconcile-results phase](phase-6-canonical-quantities-and-reconcile-results.md) owns opaque canonical-unit `ResourceBudget`, exact quantity parsing/refusal, provider-neutral capacity reads/verification, exact-budget sizing renderers, and the typed bare-Linux unsupported-storage policy. The pure foundation grants no config, provider, or plan authority and imports only lower `HostConfig`, `HostTool`, and `Substrate` families. Public exposure, the exact import-set guard, focused behavior tests, and the complete phase gate are closed |
| `HostBootstrap.Cluster.Cordon` | Implemented configuration facade | [The Dhall-configuration-and-generic-project-model phase](phase-7-dhall-configuration-and-project-model.md) owns the public facade that reexports the lower foundation and adapts `Config.Vocab.Resources`/`ResourceEnvelope`, configuration-facing preflight wrappers, and descriptive pure `fitsBudget`. It owns no plan-indexed workload/fit/partition/slice proof and imports no provider realization or cluster lifecycle module. Facade behavior tests, the exact dependency/source guard, and the complete phase gate are closed. [The cluster-lifecycle, budgets, and cordoning phase](phase-16-cluster-lifecycle-and-cordoning.md) owns exact application at cluster/direct-Colima consumers |
| `HostBootstrap.Cluster.Budget` | Implemented pure exact admission and journal-bound wall authority | Sprint 12.30's provider keys, budget/workload/partition/slice vocabulary, and exact `ProjectPlan`/resource/topology admission are closed. [The cluster-lifecycle, budgets, and cordoning phase](phase-16-cluster-lifecycle-and-cordoning.md) Sprints 16.13–16.15 replace the caller-number wall reservation and raw-observation settlement seam with a `PreparedGate`-derived reservation and backend-produced opaque permit carrying exact operation/digest/attempt/journal lineage; Sprint 16.16 adopts Sprint 6.1's canonical Colima root/data renderer while preserving Sprint 12.30's total-wall algebra. The provider-specific owning settlement is Sprint 16.31. Production recursive and demo call-site adoption remain open; Phase 24 supplies the concrete workload, overhead, partition, and slices |
| `HostBootstrap.Cluster.Lifecycle` | Implemented exact cluster package; legacy teardown callers open | [The cluster-lifecycle, budgets, and cordoning phase](phase-16-cluster-lifecycle-and-cordoning.md) Sprint 16.5 implements the opaque nominal `PlanOwnedCluster` that joins one `ProjectPlan` to its matching cluster/provider resources, retained topology, plan-derived Production or generative Harness profile/root/config presence, and exact budget slice. Its exact reconciliation route accepts no compatibility lifecycle plan, caller profile/root/envelope, or caller-built dependency snapshot. Raw legacy lifecycle/teardown call sites remain downstream Phase 17/24 adoption work |
| `HostBootstrap.Cluster.Backend` | Implemented exact clause-holding backend | [The cluster-lifecycle, budgets, and cordoning phase](phase-16-cluster-lifecycle-and-cordoning.md) Sprints 16.1–16.4 implement the private `HostTool`-resolved executor, bounded process-group runner, no-follow state/lock namespace, self-bound prepared/executing/managed origin records, exact config/private-kubeconfig snapshots, immutable control-plane and complete node identities, ID-targeted cordon, exact readiness, and identity-conditional cleanup. Production recursive and concrete demo adoption remain Phase 17/24 work |
| `HostBootstrap.Cluster.Workload` | Planned exact chart/workload transaction | Worked-demo Sprint 24.24 first adds the plan-owned `ChartWorkloadResource`; Sprint 24.25 then adds Cabal-hidden `PreparedChartWorkload` and the closed lock-held apply/observe/settle transaction plus a no-new-type exact reverse cleanup eliminator. Both return the existing generic `ReconcileResult`/`ChangeView`; callers cannot select Helm/Kubectl executables, release/namespace, values, or cleanup arguments. Sprint 24.26 is the sole readiness/activation/`PreparedGate`-joined demo adopter and attaches the cleanup projection for Sprint 24.27's exact reverse command |
| `HostBootstrap.Step` / `HostBootstrap.Chain` / `HostBootstrap.Teardown` | Implemented exact local forward/reverse substrate; recursive adoption open | Opaque steps, typed identities, reverse policy, operation keys, exact-order `StepPlan` validation, one descent declaration per frame, and declared reverse effects are implemented. The pure teardown projection preserves exact frame, node, operation, verb, ordering, and settlement evidence. Current `runChainFromFrame` is a root-coordinator-local interpreter: it consumes the exact plan, root command authority, lifecycle cursor, and root-owned ProtectedStore, revalidating the durable source before dependent actions. Production and Harness use it only for their local root frame; declared nested descent fails closed. The target recursive path does not send this store-bearing package to a child. A storeless `FrameExecutor` instead receives root-signed exact node grants, reifies only the matching root-CAS prepared gate, executes the local effect with its frame `ResourceCarrier`, and returns observations for root settlement. Root-coordinated forward and reverse traversal remain [the recursive-lifecycle-command phase](phase-17-recursive-lifecycle-command.md)'s work |
| `HostBootstrap.Lifecycle.Execution` / `HostBootstrap.Lifecycle.Dependency.Internal` | Implemented exact plan-projected descriptor; rooted grant adoption planned | The opaque `StepExecution` descriptor carries host-tool configuration, exact plan/configuration digests, stable node identity, operation key, frame, ordered dependency/projection sets, and a nominal runtime `ResourceCarrier`. `stepExecutionFor` joins one plan to its matching node, so membership and plan/configuration mismatches cannot be caller-selected. The local root interpreter currently obtains its `PreparedGate` through the root-owned durable session. The storeless `FrameExecutor` is implemented in its own Cabal-private owner: seven nominal indices of which five are minted by the opening, retaining the verified root scope, the frame's own name, plan digest, and plan nodes, its frame-local `ResourceCarrier`, and the four coordinates the root selected. One helper turns signed bytes into a response through the installed key and the exact request they answer, so no branch reads a coordinate off unverified bytes; opening admits only `Opened`, advancing admits every post-open family but `Opened` and requires a strictly greater ordinal, and execution admits only `Prepared` and reads its four packages out of that response rather than beside it. After the authorized node, its ordered dependencies, and its projections are compared against this frame's own plan and every gate package names this plan, frame, and session, it becomes the sole additional allowlisted caller of the package-private gate reifier for the durable Unknown transition the root already committed; it receives no ProtectedStore, journal, lease, snapshot, catalog row, signing key, session opener, or settlement operation. The per-frame carrier remains in-process child state and is never serialized as durability |
| `HostBootstrap.Teardown` | Implemented exact root-coordinator reverse projection; recursive traversal Active | `teardownPlan` projects the exact current frame and topology descendants deepest-frame-first and reverse-forward-order within each frame, retaining stable step/operation identity, verb, reverse policy, and declared callbacks while omitting preserved nodes. `openTeardownForest`, closed `LocalWork`/`DescentWork`, frame-bound `SubtreeSettled`, ordered observation codecs, and root-only destroy proof are implemented. A decoded child report is descriptive and cannot settle a subtree by itself. The root coordinator is the sole owner of reverse intent, prepared descent, per-frame durable state, settlement, completion, and receipt confirmation; storeless executors return observations only. Complete recursive reverse traversal, replay, cleanup, and failure adoption remain with [the recursive-lifecycle-command phase](phase-17-recursive-lifecycle-command.md), while configful migration and resource rehydration remain with [the recovery-and-migration phase](phase-18-recovery-and-migration.md) |
| `HostBootstrap.Readiness` | Implemented [canonical-quantities-and-reconcile-results](phase-6-canonical-quantities-and-reconcile-results.md) foundation, partial live integration | Opaque validated polling and total results; closed backend probes require exact planned resources and mint generative plan/resource/dependency-indexed readiness. `planDependencyProbe` registers a probe for the traversal to run at prepare time rather than binding a retained observation. `ObservedReady` is explicitly non-authorizing compatibility evidence. [The host-providers phase](phase-15-host-providers-and-the-lift.md) implements closed raw provider discovery and exact prepared Incus/Direct readiness behind backend-indexed managed authority; its static gate is closed and its native Linux/x86_64 KVM/Incus gate remains open. [The cluster-lifecycle phase](phase-16-cluster-lifecycle-and-cordoning.md) supplies the cluster live adapter, while remaining recursive and demo call-site adoption belongs to [the recursive-lifecycle-command](phase-17-recursive-lifecycle-command.md) and [worked-demo](phase-24-worked-demo.md) phases |
| `HostBootstrap.Reconcile` | Implemented generic exact descriptor and provider-start/force-cleanup transitions | `ProjectPlan` remains the sole whole-plan producer of opaque planned resources and exact edges, and the completed generic descriptor/precondition/`PreparedGate` boundary remains in force. [The cluster-lifecycle, budgets, and cordoning phase](phase-16-cluster-lifecycle-and-cordoning.md) Sprint 16.14 adds the opaque exact `PreparedProviderStart` and its private owning completion; Sprint 16.33 adds the exact-plan provider-only Running→Destroyed force-cleanup descriptor. Raw backend observations remain plan-independent, while only opaque backend results and prepared pairs may yield plan-indexed authority. Phase 24 owns the demo's concrete workload/slice/config projection; recursive production and demo call-site adoption remain downstream |
| `HostBootstrap.Protected` | Implemented | The protected, versioned record store every durable lifecycle decision compare-and-swaps against: portable OS-released exclusive entry (`hLock`), non-re-entrant sessions, atomic publish, expected-version writes/deletes, durable store identity, and a non-blocking `tryProtectedEntry`. `mkRecordName`/`recordNameIdentity` are the one injective encoding by which a **namespaced** identity — a plan operation key `core:deploy-kind`, a plan digest `<specDigest>:<planBytesDigest>` — reaches the key alphabet; its image (components with a `.`) and its plain domain (components without one) are disjoint, and a `/`-separated path of such identities — which is what a relation between operations is — encodes segment-wise joined by `..`, which no segment may contain. Two identities can therefore never share a record |
| `HostBootstrap.Authority` | Implemented producer-free safe facade and lower root kernel | The safe facade owns closed `ProjectVerb`/`LifecyclePhase` vocabulary, executable-path-verified installed identity, exact-store OS-principal evidence, and opaque root/command inspection, but no public authority producer. The hidden kernel owns root project/store-bound broker epochs, exact-scope root minting, stable reservation membership, and one-use invocation reservation. Pure frame, plan, cursor, handoff, or rooted request evidence grants no authority. Child processes receive root-signed operation grants rather than store-backed command authority, and no child route may reconstruct a root invocation id |
| `HostBootstrap.Authority.ProjectPlan` | Implemented exact root gate and child-plan evidence boundary | `authorizeRootProject` consumes the complete matching root package: invocation authority and verb, verified/bound snapshots, digest binding, lease, exact plan, root-owned acquisition journal and lifecycle cursor, and root-coordinator-resident `ValidatedLifecycleContext`. Its hidden eliminator derives the exact root frame/context and refuses nested evidence. Child construction yields only opaque `ChildPlanAuthority`, proving signed configuration and projected plan correspondence. That value grants no ProtectedStore, acquisition journal, lifecycle cursor, command reservation, `CommandAuthority`, signer, process, or effect permission. Nested execution authority is instead an exact root-signed prepared-node response consumed by the planned storeless executor |
| `HostBootstrap.Lifecycle.Mode` | Implemented root mode, lease, profile, migration, and reverse-intent durability | Production/Harness mode records, run leases, fresh and existing snapshot binding, recovered profiles, closure evidence, abandoned-run recovery, and plan-migration barriers are implemented under the one root-owned ProtectedStore. The plan-bound acquisition-journal opener and lifecycle-cursor facade retain exact project/store/lease/snapshot/broker origins. The private Down/Destroy intent protocol records exact source and target coordinates and resumes them under one root liveness extent. These records are root-coordinator state only: Mode exposes no child store opener, child cursor recovery, child command authority, or remote mutation API. The planned rooted coordinator consumes the valid root callbacks; configful forward rebuild and complete resource rehydration remain [the recovery-and-migration phase](phase-18-recovery-and-migration.md)'s work |
| `HostBootstrap.Harness.Ownership` | Implemented | The run-ownership bracket performs the abandoned-run sweep, protected Harness mode/lease acquisition, data-root acquisition/release through `Harness.DataRoot`, and generated-config acquisition/release through `Harness.GeneratedConfig`. Ownership is represented only by protected lease/origin/identity records; no pathname lock-directory protocol participates. Its package-private monotone close controller moves through unbound, binding sentinel, bound fallback, authorized pending, settled, and consumed states; the public module exposes only the opaque indexed handle, never raw close mutators or caller-supplied finalizer actions. The unbound `verifyNoProjectResourcesAcquired` proof refuses both same-run effect and acquisition rows because either is impossible ownership before binding. A bound true-pre-effect refusal may use `verifyBoundRunHasNoProjectResourcesAcquired` and the retained fallback; ordinary lifecycle completion must instead supply exact settled-destroy closure evidence before close authorization becomes pending and ownership may settle it. Persisted Closing recovery resumes its recorded authorization, while incomplete binding, effects without settled destroy, and unresolved migration revisions stay fail-closed |
| `HostBootstrap.Harness.DataRoot` | Implemented | All four § EE ownership clauses for the run's durable data root, and the first § EE backend wired into a production route: exclusive entry is the caller's `ProtectedSession`, the origin record names the exact prior identity-or-absence before the directory is created, ownership binds the created directory's stable kernel identity, and release/recovery re-observe that identity — refusing a replacement instead of deleting it. A host without a stable identity is `Unsupported` and mints no ownership |
| `HostBootstrap.Harness.GeneratedConfig` | Implemented | The same four § EE clauses over the run's generated sibling `<project>.dhall`, and the second § EE backend wired into a production route: exclusive entry is the caller's `ProtectedSession`, the origin record names the recorded absence **and the intended payload digest** before the file exists, the file is published create-if-absent, ownership binds its created kernel identity, and release/recovery unlink only on an exact re-observed identity **and** payload. A found object is refused before any mutation rather than adopted, and a host without a stable identity is `Unsupported` |
| `HostBootstrap.Harness.Identity` | Implemented | The shared clause-3 identity layer both harness ownership protocols bind to, so the directory and file realizations cannot drift: a private-constructor `ObjectIdentity`, its hex journal codec, the injected `ObjectIdentityBackend` seam, and the closed `IdentityFault` each protocol maps into its own vocabulary |
| `HostBootstrap.Harness.Identity.Native` | Implemented; native Windows gate passed 2026-08-01 as `Harness.DataRoot.Native` | The host identity backend behind that seam: POSIX `lstat` `(device, inode)`, and on Windows `GetFileInformationByHandle` over a handle opened with `FILE_FLAG_BACKUP_SEMANTICS` and locally defined `FILE_FLAG_OPEN_REPARSE_POINT`. Volume word first, little-endian, as the peer ownership backends encode it. `DataRootSpec` and `GeneratedConfigSpec` both run this backend directly |
| `HostBootstrap.Handoff` | Partial exposed authenticated-handoff facade; rooted response authentication implemented | Length-delimited framing, installed root Ed25519 identity, root-broker liveness, durable edge registration, ordinary challenge/grant verification, transport-only `VerifiedHandoff`, recovery verification, bounded lifecycle report codecs, sealed completion evidence, rooted outer tags 18/19, additive `RootedPayloadBinding`, canonical `RecoveryChildPackage` admission, and abstract nominal `AuthenticatedRootScope` are implemented. Config signing/verification requires identical exact bytes and digests. The distinct recovery-package signer consumes a hidden capability, live broker, exact recovery offer, and opaque package and derives both claims without independent config/digest inputs. The verified package join treats its supplied rooted value as untrusted signed data, cryptographically reverifies canonical bytes against the exact authenticated handoff and installed key, then decodes only the authenticated payload and recomputes both digests before exposing fields. The scope-capsule producer derives every signed field from a live broker and matching `HandoffScope`; its installed-identity/key verifier introduces Production or a fresh Harness run only through the closed rank-2 fold. The unchanged four-field Offer transport frames that capsule first, exact-verifies the rooted proof in both branches, and retains package/projection/grant/wire evidence for recovery. Root links sign only an already-constructed Offer; nested links relay canonical bytes without signing authority. This boundary composes no recovery package; the recursive-lifecycle phase's catalog is the sole producer. Sprint 13.16 implements the public lifecycle-response surface: the abstract `RootedLifecycleResponse`, renderer, fixed live-broker signer, and installed-key CPS verifier. It consumes the existing specialized hidden capability, signs only the fixed domain/key-digest/exact-request/canonical-unsigned transcript, and adds no type, generic signer, or semantic/process caller. The opaque response remains descriptive signed data and grants no broker generation, store, command, or Harness authority. Sprint 13.17's originating typed relay operation invokes that fixed verifier only after byte-preserving structural transport; the facade still exports no raw channel, generic signer, broker link, ProtectedStore operation, child receipt writer, or command authority. [The authenticated-handoff phase](phase-13-authenticated-handoff-and-child-admission.md) owns this boundary |
| `HostBootstrap.Handoff.Completion` | Implemented Cabal-private lower semantic-completion evidence | The sealed `LifecycleCompletion` foundation retains exact report/acknowledgement bytes and, for reverse completion, the exact `SubtreeSettled` proof. Completed reports construct evidence only after root-owned durable confirmation; refused and failed reports remain acknowledgement-only descriptions. The module owns no process, receiver, relay, ProtectedStore API, or child-side receipt transition and has no public constructor or testing seam |
| `HostBootstrap.Handoff.Lifecycle` | Partial Cabal-private rooted report adapter | Canonical forward/reverse completed-report rendering and exact reverse settlement joining are implemented lower substrate. The target adapter consumes root-coordinator frame completion evidence rather than a child-local authorized cursor, validates duplicated plan/frame/verb coordinates, and emits only canonical report bytes. It constructs no command authority, completion evidence, durable acknowledgement, receipt, or process state. Rooted integration remains with [the recursive-lifecycle-command phase](phase-17-recursive-lifecycle-command.md) |
| `HostBootstrap.Handoff.Protocol` | Implemented and frozen Cabal-private rooted outer framing | The bounded 19-tag codec, one-field rooted request/response tags, strict one-outstanding-request state, refusal/report/acknowledgement states, and trailing-byte rejection are implemented. Closed inner rooted values remain owned by the neutral rooted codec, not transport state; the implemented request codec has zero Protocol byte/line delta. Protocol owns no route, process, ProtectedStore mutation, broker, child scope, or raw-channel escape |
| `HostBootstrap.Handoff.Receiver` | Partial Cabal-private receiver; scope-first rooted carrier implemented | The sealed duplex receiver structurally bounds and splits the unchanged four opaque Offer fields, opens only the authentication field's leading capsule, and verifies it against the independently installed identity and key. Only inside the resulting closed rank-2 Production-or-Harness continuation does it inspect the offered key, binding, challenge/grant, or payload semantics. `withReceivedHandoffEdge` exposes only four fixed-unit config/recovery continuations, and `ReceivedEdge` retains the exact authenticated capsule beside the verified ordinary edge. `withIsolatedReceivedHandoffEdge` is the dedicated child entry over that exchange: it accepts no channel and no handle, duplicates the original standard input and output into private handles before any configuration, plan, executor, or lifecycle continuation runs, builds its binary channel from those duplicates, and for the callback's whole life redirects global standard input to the host's null device and global standard output to standard error. One outer bracket owns the duplicates and one inner bracket owns the redirection, so the globals are flushed, redirected, restored, and every duplicate closed on success, refusal, exception, and asynchronous cancellation alike, with both restores attempted independently. That is the module's only duplication policy: no ambient terminal or environment heuristic, alternate descriptor, raw channel argument, or exported testing seam exists, and it is the receiver's sole channel construction. Config admission verifies its exact rooted binding. Recovery admission retains the verified rooted binding, opaque `RecoveryChildPackage`, typed projection and grant, and verified adapter wire; its existing eliminator derives legacy raw views from those values. The 7 MiB embedded Offer-payload ceiling is only a strict sub-ceiling; Protocol's 8 MiB total-body check remains authoritative. The carrier exposes no ProtectedStore, durable cursor, command authority, signing key, or receipt writer. Rooted request/response exchange remains closed and package-owned; [the recursive-lifecycle-command phase](phase-17-recursive-lifecycle-command.md) alone constructs the storeless executor |
| `HostBootstrap.Handoff.Relay` | Partial Cabal-private authenticated relay; transport-only rooted carrier implemented | `rootBrokerLink` consumes the matching `HandoffScope`, mints one live-broker-signed `AuthenticatedRootScope`, and retains it in `BrokerLink`; every nested link copies only the exact capsule retained by its authenticated parent edge. Rooted signing begins only after the exact `HandoffOffer` exists: the root signs through its live broker, while a nested link relays canonical private request/response bytes and receives no signing capability. Config authentication has exact capsule/key/rooted fields; recovery has exact capsule/key/rooted/projection/grant fields. The reverse route wraps the durable Bound transition of one prepared descent: it bounds the embedded payload, opens the complete catalog-produced recovery package recoverably through the frame's own keyless link, proves payload/token/binding agreement, and only then routes the exact Offer to the already-installed root signer and serves the existing challenge loop. It takes no payload argument, so an adapter alone is unrepresentable there, and a repeated attempt recovers the binding and token the root already minted. Ordinary parent/child ancestry validation, root-routed grant/recovery signing, canonical report transport, and exact request/response matching remain implemented lower substrate. The rooted carrier uses singleton outer fields, bounded sealed external requester-envelope construction, exact inner-byte preservation, structural one-request/one-response pairing, intermediate suffix checks, root complete-path equality, and originating installed-key verification. Its root endpoint validates structure and then runs the rooted lifecycle service `rootBrokerLink` is constructed with; that endpoint's shape is the link field's own, so a hop reads no more than before, and an outer refusal and any exact signed rooted refusal are still transported without reinterpretation. The one service the repository builds is the opening call site, which binds the root arm, requires the installed runtime key to be the live broker's own route digest, hands the session owner a signing continuation, and reaches the fixed signer only through the hidden recovery signing admission this module already holds. Intermediate and child arms hold no capsule signer, project signing key, root broker, Activation broker, ProtectedStore, root catalog, retained session path, semantic replay/receipt decision, process owner, or generic storage RPC. [The recursive-lifecycle-command phase](phase-17-recursive-lifecycle-command.md) owns storeless execution and the first successful rooted process exchange The parent half now bounds only the frames a peer owes immediately: the challenge and the acceptance of the offered digest are received under a fixed control-frame deadline whose expiry is its own closed refusal, while the wait that follows admission — the one that lasts as long as the child's admitted backend effect — stays unbounded, because a single wall-clock deadline across it would turn every slow provisioning step into a protocol failure |
| `HostBootstrap.Handoff.Recovery` | Implemented neutral recovery-package codec | Opaque `RecoveryChildPackage` carries exactly two non-empty frames for child configuration and adapter under one complete canonical 8 MiB bound. Strict decode and field elimination remain Cabal-private; the public facade exposes only the abstract value and canonical renderer. The module imports no handoff authority, cryptography, ProtectedStore, receiver, or catalog owner. The generic receiver carrier consumes it, but no application or catalog producer constructs a real package |
| `HostBootstrap.Handoff.Rooted` | Implemented neutral rooted binding and lifecycle request/response codecs | Opaque `RootedPayloadBinding` exact-binds the byte-stable immediate-edge bytes and separately frames complete-payload and child-config-field digest claims. Its neutral codec canonicalizes unsigned bytes and imports no `Command`, `Chain`, process, facade, or ProtectedStore owner. Facade config signing consumes only a fixed-domain hidden admission plus live root broker and requires exact payload/config equality; the separate recovery-package signer derives the complete-package and extracted-config claims from one checked package. A signed unequal recovery claim alone admits neither; only cryptographic reverification against the exact ordinary handoff followed by package decoding and digest recomputation admits its fields. The one non-indexed closed request is implemented in this existing hidden module: four fields for pathless `OpenFrame`, nine for post-open control, and ten only for the opaque settlement/descent-result body, with strict canonical rerendering and a 7 MiB complete bound. The implemented descriptive non-indexed seven-case response has exact nine-field `Opened`, exact eleven-field post-open forms, a 6 MiB body/7 MiB total bound, seven checked unsigned builders, one signature-attaching decoder, one closed fold, and structural exact-request-family pairing. `Prepared` frames node/dependencies/operation-gate/projected-gates packages; `FrameComplete` remains opaque here; `ReceiptRecorded` repeats its matching predecessor digest; rooted `Refused` is post-open only. Exact source guards keep authority, cryptography, facade, testing companions, and semantic/process/durable importers absent; only the neutral Receiver-internal path/pair folds serve Relay transport. [The recursive-lifecycle-command phase](phase-17-recursive-lifecycle-command.md) alone assigns semantics |
| `HostBootstrap.Handoff.Runtime` | Implemented installed recursive-handoff runtime | Hidden all-nominal `RecursiveHandoffRuntime scope brokerGeneration verb` has two arms. Both retain the derived installed project, scope tag, protected-store identity, broker generation, installed verification-key digest, and closed verb; only the nested arm also retains the authenticated frame its keyless relay speaks for. The root arm derives every coordinate from the admitted root environment — invocation authority, matching `HandoffScope`, and the live broker's own public half — cross-checks that digest against the route's advertised one, and requires a route with no authenticated current frame, so the path-agnostic root arm and the frame-bearing nested arm are mutually unreachable. The nested arm derives its coordinates from an already authenticated parent edge and requires the relayed route to name exactly that edge's child frame; both arms refuse an empty coordinate or a zero generation. The lifecycle-only policy is absence rather than a filter: the module names no protocol tag, activation manifest, build key, channel, `BrokerLink`, requester path, catalog, session, journal, compare-and-swap, ProtectedStore, or signing key, and imports no effect or transport owner. Its general fixed-unit eliminator discloses only whether this frame signs locally plus those coordinates, and a second fixed-unit fold admits the root arm alone — it discloses no authenticated frame, because a root has none, and a keyless nested arm cannot be read through it at all, so the arm that may reach a signer is chosen by which fold a caller can enter rather than by a boolean beside the identity. A third fixed-unit fold is that narrowing's mirror: it admits the keyless nested arm alone and discloses the authenticated frame itself rather than a `Maybe`, so a caller about to open a child's protocol channel has no case to write for a runtime that speaks for no authenticated frame. `Handoff.Relay` hands a nested runtime out only beside the keyless relayed link derived from the same edge; `Command.LifecycleEntry` installs the root arm only from a sealed root entry and refuses both child entries. Neither producer is reached from a command interpreter |
| `HostBootstrap.Handoff.Process.Route` | Implemented protocol-safe lifecycle process route | Hidden all-nominal `LifecycleProcessRoute scope rootPlanId brokerGeneration catalogId parent child verb` is derived from a catalog forward package or a recovery package together with its plan-owned lift route, never assembled, and points only at the child a frame launches rather than at that frame's own session: the parent and child frames are the binding input's own and must be a real descent, the phase must be the one that edge belongs to, and the lift must be exactly one plan-owned layer. It renders one argument vector per provider — Docker keeps standard input attached and runs at `/`; Incus, Lima, and WSL run noninteractively at `/`, reaching root through noninteractive sudo where the guest's default user is not root — and places only the invocation's own closed verb under `project`. Its closed grammar refuses `ConfigDelivery`, container extra arguments, a container that outlives its exchange, a non-absolute or delimiter-bearing path, and any derived name reading as an option, a separator, or a descriptor request, so the detach, TTY, attach, standard-input, entrypoint, working-directory, and signal overrides are unrepresentable rather than filtered. Beside the route the same module owns the one startup step with no other owner: a frame's own opening, which is not a fold on the route. It admits the nested arm of that frame's `RecursiveHandoffRuntime`, builds the four-field `OpenFrame` from a fresh nonce alone, carries it through the frame's own carrier, and verifies the signed answer against the independently installed key and those exact request bytes, admitting only an `Opened` and yielding that exact request and that exact signed response with no decoded coordinate. No post-open request builder exists here: `Lifecycle.FrameExecutor` already owns the root-selected path, session, stage, ordinal, and predecessor and the closed post-open families. It names no process, descriptor, handle, ProtectedStore, signer, catalog row, or executor and imports no effect or transport owner; `Handoff.Process` is its sole importer |
| `HostBootstrap.Handoff.Process` | Implemented POSIX bracketed child-process owner; command adoption open | The POSIX-only owner spawns exactly one sanitized `LifecycleProcessRoute` and nothing else: its host tool is resolved to an absolute path through the installed configuration, so a bare command name cannot be executed even if one reached the argument vector, and an already opened route refuses before a process exists. The child runs in a new process group with standard input and output on private pipes and standard error inherited, which is where the isolated receiver sends everything that is not a protocol frame. One bracket owns those descriptors, the relay's lifetime, the fixed completion kernel for the edge's direction — forward or Bound reverse, never a caller-selected one — and the reap. Its release path runs on normal return, refusal, protocol failure, exception, and asynchronous cancellation alike: group TERM, a fixed grace, group KILL if the group is still present, an unconditional wait, and only then the pipes closed, so a shell or provider the child launched goes away with it. No caller supplies a callback, deadline, grace value, process shape, or channel. The owner's constants bound the launch and the grace only; `Handoff.Relay` bounds the frames a peer owes immediately and leaves the wait between admission and the completed report unbounded, because that wait is the admitted backend effect and the effect keeps its own closed policy. EOF, exit zero, diagnostic text, and channel closure are not completion: only the rooted completion the relay obtained and the root recorded is reported as success. It names no ProtectedStore, record key, broker, signing key, catalog, or executor, and has no command call site |
| `HostBootstrap.Lifecycle.Rooted.Node` | Implemented node grant and exactly-once settlement | The node owner reaches a frame session only through that session's fixed-unit coordinate fold, so it never receives the session's record key, version, or row bytes and cannot advance or rewrite the session itself; every durable row it writes is one it derived a key for. Preparation publishes and strictly reads back every exact unknown row — the node's own operation first, then each projected operation in the catalog's order — before the grant exists. Settlement checks requester path, session token, ordinal, nonce, and the session's recorded predecessor before any write; only `SettleNode` and `DescendResult` settle, and only against a supplied signed response in the paired `Settled | Refused` family that echoes the same path/session/nonce and selects a strictly greater successor ordinal. The observation row is published and strictly read back before the response digest is taken, and its key carries the session ordinal, so an exact retry converges, a different observation refuses without effect, and one node settled at two ordinals addresses two rows. It produces and signs no response, mints no gate, and creates no local execution authority |
| `HostBootstrap.Lifecycle.Rooted.Receipt` | Implemented terminal report and receipt confirmation | The receipt owner holds how one frame session ends, and it reaches a session only through that session's fixed-unit coordinate fold, so it never receives the session's record key, version, or row bytes. It goes one step further than the node owner: it names no `ProtectedStore` at all. Publishing the exact canonical report and advancing Published to Received both arrive as continuations from the module that already holds the hidden recovery signing admission, so what this owner holds is the join and the two digests rather than the writes. Only a `CloseFrame` reaches a terminal report and only a `ReceiptConfirm` confirms one; every other request form leaves through one fixed continuation, and each answer must belong to its own paired `FrameComplete | Refused` or `ReceiptRecorded | Refused` family, with a signed `Refused` read as an outcome rather than minted. The close checks requester path, session token, ordinal, nonce, and the predecessor the session recorded when it attached, then requires the carried report to eliminate canonically and name the session's own verb; the report is published and strictly read back before the complete signed-response digest exists. The confirmation names that report by carrying the digest as its predecessor, only an exact match runs the compare-and-swap, and a recorded receipt repeats the same digest in its own body. Neither exchange retains state of its own, so an exact retry re-derives the same digest over a row already where it belongs and a child persists no receipt state |
| `HostBootstrap.Lifecycle.Rooted` | Partial root coordinator; frame session implemented | Hidden seven-role `RootedFrameSession scope rootPlanId brokerGeneration catalogId frame sessionId verb` is the sole owner's only named type, and its two constructors nest as the durable states do: Opened retains the root-selected coordinates and the exact row they were published as, Attached nests that opening with the admitted nonce, first predecessor digest, and successor row. Opening admits before it writes — the runtime must be the path-agnostic root arm, so a keyless nested arm cannot open a session at all. The requester path is the catalog's own descent chain to the frame, root-nearest-to-leaf, derived through the existing entries and root folds; a frame no admitted edge names, a frame two edges name, and a chain that overruns the catalog all refuse. The session token is an opaque stable digest of root lineage, catalog identity, frame, verb, and path, the stage is fixed, the next ordinal is the first nonzero one, and the record key is a function of root lineage, catalog identity, and frame alone. The published row carries no predecessor, and the opening refuses unless the strict readback is byte-identical to what it rendered. Attachment resolves the root arm, the sealed external envelope against the session-retained path, and the closed request family before mutation: only `OpenFrame` attaches and every post-open form refuses. The attached row frames root lineage, catalog identity, envelope path, and nonce together, so that quadruple is the replay identity. The first predecessor is the lowercase SHA-256 digest of the complete signed `Opened` bytes as supplied. The same owner holds the opening join: it binds the root arm through the runtime's root-only fold, enforces the sealed envelope's one-to-256 component and 4,096-byte encoded-component grammar before comparing that envelope to the path the session retains, admits exactly an `OpenFrame`, and renders the nine-field unsigned `Opened` from root-selected coordinates alone, so the request contributes only the digest that names it. Signing arrives as a continuation, so the module still names no signer and the attachment records and reads back the complete signed response's digest before the caller may release those bytes; because Ed25519 signing is deterministic, an exact replay re-derives the same bytes over a row already attached and any other bytes are a conflicting row rather than a second opening. Every failure is pre-attachment and leaves the transport's existing outer `Refused` to carry it; no rooted `Refused` is constructible here. Later requests map onto the one root ProtectedStore, global invocation lease/snapshot/acquisition state, recursive catalog, per-frame journals, prepare/settle transitions, completion, and receipt confirmation. `PreparedNodeGrant` follows exact durable Unknown; no raw CAS/store RPC crosses the transport |
| `HostBootstrap.Lifecycle.FrameExecutor` | Implemented long-lived storeless frame interpreter | A `FrameExecutor` is constructed only after exact installed-key verification of `Opened` and owns only its signed canonical path/sequencing state, frame-local execution package, and `ResourceCarrier`. It verifies each root-signed response, checks exact frame plus node/dependencies/operation-gate/projected-gates package equality, reifies the corresponding prepared gates, performs the local effect, and reports observations. It has no ProtectedStore, invocation lease, acquisition journal, lifecycle cursor, root command authority, root signer, session opener, or generic persistence capability |
| `HostBootstrap.Activation` | Implemented protocol; deployment consumer open | Runtime role activation uses its own independently provisioned long-lived Activation signing/verification identity and the `hostbootstrap/activation/v1` signature domain. A runtime-closed `ActivationBroker` signs only the exact canonical manifests in its closed policy; an escaped broker refuses. `ActivationManifest` binds the immutable rollout revision and every pre-instantiation index but deliberately no instance identity. Its dedicated private-bundle coordinate is an opaque canonical SHA-256 digest computed from bytes. Startup supplies its own `RuntimeMeasurement` (binary, mounted wire, private bundle, non-empty pod UID plus restart count or non-empty host invocation nonce), and verification against an independently selected exact manifest and protected-store origin yields the inseparable `VerifiedRuntimeRoleActivation`. Activation itself confers no lifecycle authority. [The composition-and-network-algebra phase](phase-21-composition-and-network-algebra.md) owns the sole one-use admission and consuming role lifecycle, and [the service-runtime phase](phase-22-service-runtime.md) owns the production service gate |
| `HostBootstrap.Build` | Implemented protocol; consumer open | The attesting image-build gate uses a distinct independently provisioned long-lived Build signing/verification identity. A runtime-closed coordinator signs the canonical `BuildBinding` under `hostbootstrap/build/v1`; an escaped coordinator refuses. The grant binds source-root and builder-path measurements and arrives through a coordinator channel whose absence is an explicit refusal, not a fallback. Verification measures the caller-supplied paths and jointly yields `ImageBuildFrame` and `BuildInvocationAuthority`, from which only per-returned-authority, at-most-once `CheckCodePhase`/`BuildPhase` authorities can be derived. It neither establishes that those paths are the engine context/running executable nor globally consumes a signed file across verifier calls. No function accepts a `BinaryContext`, so the baked image-build config reaches none of it. Current `HostBootstrap.Command.checkCodeCommand` does not consume this authority. Its static consumer seam, trusted path derivation, single presentation/acknowledgement or durable `buildId` replay refusal, delivery through the demo Docker build, and live container evidence belong to [the worked-demo phase](phase-24-worked-demo.md); ordinary developer `check-code` remains distinct |
| `HostBootstrap.Lifecycle.Prepared` | Implemented durable-Unknown gate invariant | `PreparedGate` hides its constructor. Every package-private producer is admitted only after the exact operation's durable Unknown compare-and-swap; the source guard allowlists each producer. The local root session mints the gate directly, while the planned storeless executor may only reify the same gate after verifying a root-signed response and exact local node/projected-key equality. No caller literal or child-local store can manufacture prepared authority. The same hidden owner now also holds the eight-role `PreparedNodeGrant scope rootPlanId brokerGeneration catalogId frame sessionId node verb`: it retains the canonical signed response, the authorized node, that node's ordered dependencies, and the operation-gate and projected-gates packages, and names no store, session, record key, or compare-and-swap operation, so its fixed-unit evidence fold discloses what a frame may do and nothing about how the root recorded it. Its producer cannot reach the constructor until every exact durable unknown row is compare-and-swap published and strictly read back — the node's own operation first, then each projected operation in the catalog's order — and a row returning anything but the exact bytes rendered for it stops the whole preparation. Duplicate, self-referential, and empty projected keys refuse before the first write. The grant carries gate packages rather than `PreparedGate` values, so it mints nothing and the three-owner mint allowlist is unchanged, and only a `NextNode -> Prepared` answer has a constructor on that path |
| `HostBootstrap.Lifecycle.Session` | Implemented root-owned journal, cursor, prepare, and settlement foundation | Protected operation sessions, idempotent fencing, recovery classification, acquisition journals, strict per-frame lifecycle cursors, one-use command reservation, prepare compare-and-swap, settlement, and close are implemented. Every durable value retains exact project, store, lease, plan, frame, verb, phase, broker, record-version, and byte origins; dependent actions revalidate those origins in the same protected entry. These APIs are root-coordinator durability only. A child process receives neither `AcquisitionJournal` nor `LifecycleCursor` nor `ProtectedStore`; the planned rooted session maps signed requests onto these exact root records and returns bounded grants/results |
| `HostBootstrap.Context` / `HostBootstrap.Lifecycle.Context` | Implemented descriptive context and root-coordinator-resident lifecycle evidence | Descriptive binary context, topology validation, placement/witness relations, and pure plan-bound `CurrentFrame`/`ProjectFrame`/`ValidatedContext` admission are implemented. `ValidatedLifecycleContext` joins the plan-retained canonical root, already-open root ProtectedStore identity, installed project/binary identity, exact topology, placement, and frame. Its hidden root/nested eliminators allow the root coordinator to reason about every catalog frame, but the value remains resident in the root process and is never child-process input. It grants no command, journal, cursor, handoff, signing, or backend-effect authority. A storeless child receives only the signed coordinates and execution package derived from this root evidence |
| `HostBootstrap.ProjectRoot` | Implemented foundation and retained plan input | Private rank-2 canonical-root admission retains the surrounding config/lifecycle scope and mints only the fresh `rootId`; sibling admission therefore returns `CanonicalProjectRoot configScope rootId`. Same-root host durable projection and the typed direct-host mount adapter are implemented. `canonicalHostSubPath` derives a host path under the admitted root from supplied segments and rejects non-components. The opaque `ProjectPlan` retains the exact admitted root and owns resource projections; Production and Harness retain that plan through the hidden root-Up entry and exact reverse boundaries, while public Chain remains their lower interpreter |
| `HostBootstrap.Substrate.Provider` | Implemented boundary; native phase gate open | `SubstrateProvider` is an opaque descriptive value selected from the closed Incus/Lima/WSL2/Direct kind. Its closed request plans accept only raw exit/stream/transport outcomes, parse strict single-line reports privately, poll only bounded `NotReady`, and mint a generative backend-indexed capability only from the exact opaque managed Running provider. `Provider.Reconcile` exposes opaque nominal `ManagedProviderHandle` and `ManagedProviderShareHandle` authorities plus prepared provision/ready/share/stop/delete operations. The Incus backend holds one `flock` namespace, publishes and binds durable provider/share origins, and conditionally mutates only the exact UUID/nonce identity. Direct settles only a plan-local reservation and identity share; stop, delete, guest execution, and guest alias are structured refusals with no physical-host mutation. `Provider.Alias` consumes the exact managed provider/share authorities, settles only to an opaque nominal `ManagedGuestAliasHandle`, admits only retained guest `flock` (a discovered `lockf` remains descriptive `Unsupported`), and recovers durable `prepared`/`managed`/`releasing` records before identity-conditional release. The static gate is closed; the native Linux/x86_64 KVM/Incus gate remains open, and the demo's exact provider and prepared-alias call-site adoption remains [the worked-demo phase](phase-24-worked-demo.md) |
| `HostBootstrap.Lift.Context` | Implemented public pure boundary | [The Dhall-configuration-and-generic-project-model phase](phase-7-dhall-configuration-and-project-model.md) owns the Incus/Lima/WSL2 target records, container/config-delivery data, outermost-first `LiftContext`, canonical incremental constructors, same-root `canonicalHostMount`, and exact inner transport argv renderers. The data constructors remain public for inspection and exact fixture construction. The module performs no tool resolution or effect and imports no later plan, provider-realization, Registry, or cluster module. Public exposure, `LiftContextSpec`, compile-fail mount cases, the lower-boundary source guard, and the complete phase gate are closed |
| `HostBootstrap.Lift` | Implemented generic dispatch boundary | [The ensure-reconcilers phase](phase-8-ensure-reconcilers.md) owns the provider-neutral resolved-tool fold/effect dispatch and reexports `Lift.Context`. It resolves only the outer host tool, streams config delivery, and owns no provider realization or Registry policy. Fold, effect-seam, adversarial shell-quoting, decorated/multiline-aware exact import-guard coverage, and the complete phase gate are closed. `reachLeaf` and the blob leaf smart constructors are later additive extensions owned by [the composition-and-network-algebra phase](phase-21-composition-and-network-algebra.md), not part of the generic fold contract |
| `HostBootstrap.Incus` / `Lima` / `Wsl2` | Implemented provider realization boundary; native phase gate open | [The host-providers-and-self-reference-lift phase](phase-15-host-providers-and-the-lift.md) owns lifecycle probes/builders that consume and reexport the lower target records and inner renderers. Incus has the baseline prepared, four-clause provider/share backend; Lima and WSL2 consume the same opaque provider descriptor, closed discovery, and lower rendering contracts while their native confirmation remains in their terminal substrate acceptance phases. The production WSL utility-VM wall enters through its separate journalled host-wall driver and releases on teardown. Reexport/import-direction guards, focused provider coverage, and the host-provider static gate are closed; the native Linux/x86_64 KVM/Incus gate and later demo call-site adoption stay open |
| `HostBootstrap.Wsl2.GlobalWall` | Implemented production authority and recovery driver | Exact present/absent origin, durable unknown phases, identity-bound stage/apply/restore classification, fencing, opaque receipt/authority values, and conflict-only recovery are consumed by the production `ApplyGlobalWslWall`/`ReleaseGlobalWslWall` route. The shared model/codec gate is portable, the full driver gate runs on POSIX, and a focused native Windows production-entrypoint gate passed 4/4 on 2026-08-01 |
| `HostBootstrap.Wsl2.GlobalWall.ConfigBytes` | Implemented, retained | Strict bounded UTF-8/UTF-16 transformation with idempotence fixtures. Portable and unaffected by the ownership restatement; carried forward unchanged |
| `HostBootstrap.Wsl2.GlobalWall.Windows` | Implemented production backend; focused native gate passed 2026-08-01 | Windows realization of the portable host-wall driver. Public `Win32` types/wrappers cover ordinary operations; a narrow direct `kernel32` FFI preserves exact status for ownership-critical handle and namespace calls. The package has no private `Win32` module, C shim/source, Cabal `c-sources` block, or threaded-RTS carve-out |
| `HostBootstrap.Harness` | Implemented typed assertion engine, ownership, and exact-plan interpretation | Opaque typed case/variant matrix, selection, reporting, and exclusive run ownership are implemented. `CaseResult` distinguishes the project's own `Fail` from the engine-classified `Refused`, `LifecycleFailed`, and `TeardownFailed`; `HarnessRunOwnership` supplies receipt-driven cleanup and Harness close. The five-field `TestSuite` contains only its safety precondition, per-variant assertion-environment opener, typed cases, case assertion, and post-reverse assertion—there is no project lifecycle callback or self-invocation route. For each selected variant, `Command` retains one exact `ProjectPlan (Harness projectId runId) ...` through generated-config ownership and supplies the engine an opaque `HarnessLifecycle` backed by the Cabal-private fixed root-Up entry and exact reverse. That entry alone derives the lower Chain's journal/cursor/authority inputs. Its constructor lives in a private Cabal component; the public facade exposes only the opaque value and eliminators, so downstream suites cannot forge another lifecycle interpreter. True pre-effect refusal skips reverse only after the bound no-resource proof; acquired failures reverse, and ordinary terminal close requires settled-destroy closure evidence. [The recursive-lifecycle-command phase](phase-17-recursive-lifecycle-command.md) still owns proof-complete recursive traversal and a same-run restart transition; the demo durable destroy/up/readback assertion remains honestly open in [the worked-demo phase](phase-24-worked-demo.md), and [the test-harness-and-run-ownership phase](phase-19-test-harness-and-run-ownership.md) owns remaining interruption/live acceptance |
| `HostBootstrap.Service` / `HostBootstrap.Service.Internal` | Implemented typed codec/request boundary, partial runtime | Closed typed registry definitions bind identity/projection/role codec/handler; finalization shares one digest with the full codec, service dispatch verifies one snapshot, and a handler's whole input is `ServiceHandler fields` = `RoleParams specDigest configId secretDigest fields service -> IO ()` — its own role's opaque bundle, with no framework view, so a role that needs a framework datum declares it as a field its own projection supplies. Replacing the handler's raw `IO` with one-use effect-indexed execution remains [the service-runtime phase](phase-22-service-runtime.md)'s work; [the composition-and-network-algebra phase](phase-21-composition-and-network-algebra.md) supplies the phase lifecycle. The hidden owner holds the `ServiceId`, handler, finalized-definition, and `FinalizedServiceRegistry` representations plus the one reindex kernel that relabels the nominal specification phantom under the digest-equality token, after proving the token's digest equals the digest the registry and every role codec retain; the facade re-exports the abstract type and every producer and eliminator unchanged. Native accelerator real-run evidence belongs to the corresponding substrate acceptance phases |
| `HostBootstrap.RoleLifecycle` | Implemented engine, call site open | The phase-indexed role lifecycle ([the composition-and-network-algebra phase](phase-21-composition-and-network-algebra.md), 2026-07-30). It also carries the declared effect row: `RoleEffect` promotes, `EffectName` is its per-effect tag, `DeclaredEffects effects` is a type-level row whose term-level twin agrees by construction, and `HasEffect` has no empty-row equation so an undeclared effect is an unsolved constraint rather than a runtime refusal. `authorizeServiceEffects` is the sole producer of `EffectAuthorization … effects`, admitting a declared row only within the signed `permittedEffects` ceiling and recomputing the lease requirement from the declaration. The public `RoleSpec`/`runRole` callback bag is absent. A role passes `verifyRolePlanDraft` (no durable mutation) → `withRoleLifecycleAdmission` (the sole one-use reservation, using a bounded legal `role-admission.<sha256>` key over domain-separated, length-framed exact plan/frame/revision/measured-instance coordinates) → `withRuntimeRolePlan` (CAS-consumes that reservation, mints `RolePlan`/`RolePlanDigestBinding`/`VerifiedServicePlacement` and the sole `Prereq` cursor), after which the core-owned engine privately drives Prereq → Acquire → Ready → Serve → Drain → Exit and returns only `RoleExitReport`. Admission, plan opening, and engine entry each refuse a protected store different from the activation's privately retained origin before their respective durable, liveness, or engine effects. The current API prevents duplicate use but does not rehydrate an existing reservation or consumed plan after asynchronous/callback acknowledgement loss; Phase 21 owns that recovery protocol. The lease requirement is derived from the signed effect ceiling, and the exclusive branch holds a kernel lock across Acquire→Drain. Production `service run` entry requires the proof-complete root and recursive command path owned by [the recursive-lifecycle-command phase](phase-17-recursive-lifecycle-command.md); [the service-runtime phase](phase-22-service-runtime.md) owns the effect-indexed selected-service package and call-site adoption |
| `HostBootstrap.Service.Program` | Implemented program/interpreter, registry adoption open | The closed effect-indexed `ServiceProgram payload service effects a` a handler returns. Constructors are private and there is no `IO`/`MonadIO` constructor, so a project builds one only through the smart constructors and the `Monad` instance and cannot write a second interpreter; `interpretServiceProgram` is the sole eliminator and demands the `EffectAuthorization`. Payload types sit under one `payload` index with associated types, so a program and its backend agree by one type equality. Every listener/peer/worker argument is an `AcquiredResource service` the Ready phase alone produces. `DurableStore` is core-executed against a `DurablePath` minted only through `canonicalHostSubPath`; the other three families reach an injected `ServiceBackend`, the `ClusterExec`/`GuestExec` boundary. There is no unauthorized-effect failure: the indexed row and the authorized row agree by construction. `serviceDefinition` carries its declared effect row through selection, but handlers still return raw `IO` and no call site builds a backend; [the service-runtime phase](phase-22-service-runtime.md) owns that adoption |
| `HostBootstrap.Config.*` (including hidden `Config.Class.Internal`, `Config.Schema.Internal`) | Implemented config/role, exact config-handoff, and scope-first rooted receiver boundary | Generic scope-indexed config classes, opaque secret references, canonical verification, common framework view, full-versus-role discriminators, `RoleCodec`, requests, and role parameters are implemented. Config handoff refinement joins transport verification, the separately committed child-configuration digest, exact verified wire/config bytes, project verb, and closed phase before yielding fully indexed configuration evidence. The Offer/Relay/Receiver path carries and verifies the abstract `AuthenticatedRootScope` before the rank-2 continuation introduces the child scope or performs binding/payload semantics, then verifies the exact rooted proof before config bytes are exposed. Recovery additionally joins the canonical package, projection, grant, and authenticated extracted adapter without retaining caller-supplied raw forms. `ChildPlanAuthority` derived from the existing refinement proves projected configuration/plan agreement only and confers no ProtectedStore, cursor, command, signer, or effect authority. `Config.Class.Internal` owns the `ProjectCodec` representation and its one reindex kernel; `Config.Schema.Internal` owns `ValidatedConfig`, the `RecoverySpecReindex` digest-equality token minted only from an equal digest pair, and the accessor through which every carrier reads its target digest. Live storeless execution and the first real recovery-package producer belong to [the recursive-lifecycle-command phase](phase-17-recursive-lifecycle-command.md) |
| `HostBootstrap.Dhall.*` | Implemented foundation | Opaque `CodecWitness` owns schema/decode/render, opaque artifacts require an admitted codec, literal schema commands are snapshotted, every current `Core.dhall` type export is equality-owned, and [the Dhall-configuration-and-generic-project-model phase](phase-7-dhall-configuration-and-project-model.md)'s `ProjectCodec` supplies installed identity/scope/spec-digest binding |
| `HostBootstrap.Registry` | Implemented additive Lift ownership; stronger credential capability open | Docker Hub credential discovery/forwarding exists, but still exposes raw text, classifies registry keys by substring, and uses environment transport. Registry owns `liftSubcommandWithAuth`, depends on lower generic Lift, and is guarded so Lift imports no Registry policy. [The recursive-lifecycle-command phase](phase-17-recursive-lifecycle-command.md) owns the scope/registry/credential-indexed capability and root-coordinated keyless transport across authenticated descent; [the worked-demo phase](phase-24-worked-demo.md) owns demo adoption and live confirmation. Schema/artifact registration lives in `HostBootstrap.Dhall.Gen` |
| `HostBootstrap.Network` / `HostBootstrap.RegistryPlan` | Implemented generic algebra and additive Lift helpers; blob tests and phase gate open | [The composition-and-network-algebra phase](phase-21-composition-and-network-algebra.md) supplies scope-indexed endpoints/clients/exposures, proof-gated blob delivery, opaque finalized registry plans, route-specific readiness, and the later `reachLeaf`/blob leaf helpers over generic Lift. `reachLeaf` coverage and the Registry-to-Lift direction guard are implemented; exact argument-shape coverage for the four blob leaves and the fresh phase gate remain open. [The worked-demo phase](phase-24-worked-demo.md) consumes the algebra in the demo |
| `HostBootstrap.DocValidator` | Implemented | Mechanical documentation checks; new drift floors belong to [the documentation-reconciliation phase](phase-28-documentation-reconciliation.md) |

Ownership across this inventory is deliberately split at the consumer boundary. Phase 12 owns the exact
`ProjectPlan`/current-frame/Chain and Production `Command` current-frame foundation; Phase 17 owns
the recursive root coordinator, catalog, storeless executor, and traversal. Phase 13 owns the
root-authenticated wire used between them. Phase 19 owns the Harness
`Command` consumer and assertion engine; its direct interpretation of the common plan is not Phase 12
call-site work.

Phase 12's snapshot/recovery boundary is intentionally split from the current mode primitives. Through
Sprint 12.20, version-3 root-bound public stable bytes, pure `PlanDigestBinding` verification, non-authorizing
`BoundPlanSnapshot` evidence, origin-checked fresh persistence/binding, and fully indexed existing
Production admission, pure recovered-profile refinement, and exact recovered-plan reconstruction are
implemented. Reconstruction safely joins an independently finalized config/draft candidate before reproducing
the protected bytes over the existing
`BoundInvocationRecovery scope specDigest planDigest planId brokerGeneration`. Fresh persistence stores,
flushes, reads back, and verifies the private indexed snapshot before a separate lease-record
compare-and-swap. Those are ordered durable transitions, not an atomic multi-record transaction; the
intermediate persisted-snapshot/unbound-lease stage is classified. Existing-snapshot admission generates
the sole local `planId`, which recovery classification, the recovered profile, and reconstruction retain
without another quantifier or mint.

## Lifecycle Type Contract

The current Phase 12 construction/projection path through Sprint 12.26 is:

```text
ProjectSpec cfg tcfg
  -> FinalizedProjectSpec scope specDigest cfg
  -> NonEmpty (PlanDraft scope specDigest (cfg scope))
  -> ProjectPlan scope specDigest planId configId cfg
  -> forward / topology / CurrentFrame package
       / plan-owned resources and dependency edges
  -> StablePlanSnapshot
  -> VerifiedPlanSnapshot / BoundPlanSnapshot / PlanDigestBinding
  -> BoundRunLease / NormalActiveRecovery

ProjectPlan scope specDigest planId configId cfg
  + CurrentFrame scope planId frame
  + ProjectVerb verb
  -> TeardownPlan scope planId frame verb
  -> openTeardownForest
  -> TeardownForest scope planId frame verb
       (the existing frame is retained through every successor;
        LocalWork/DescentWork and ProjectVerb unification are implemented;
        frame-bound subtree settlement and root-only destroy proof are implemented in Sprint 17.6)

ProtectedStore + InstalledProjectIdentity
  -> terminal InvocationCloseKey
   | Open root / Production mode / existing BoundRunLease
          / VerifiedPlanSnapshot / BoundPlanSnapshot / PlanDigestBinding
          / BoundInvocationRecovery scope specDigest planDigest planId brokerGeneration
          -> RecoveredProductionLifecycleProfile
               projectId specDigest planDigest planId brokerGeneration
          -> independently finalized matching config + regenerated drafts
          -> ProjectPlan
               (Production projectId) specDigest planId configId cfg
          -> ValidatedLifecycleContext scope specDigest planId configId frame
          -> hidden root-Up LifecycleEntry producer
               -> AcquisitionJournal scope planId brokerGeneration
               -> LifecycleCursor scope planId frame brokerGeneration VerbUp phase
               -> authorizeRootProject
               -> CommandAuthority scope planId frame brokerGeneration VerbUp phase
```

The static `ProjectSpec` carries no runtime identity. The finalizer supplies scope/specification, and
fresh plan admission supplies `planId`; callers cannot choose either. Reconciliation's exact descriptor
producer consumes the admitted plan and one matching projected node. Public Chain consumes that exact
plan's `forward`/`DerivedTopology` projections plus matching execute-phase command authority and cursor,
checks the supplied store and the cursor's retained store plus decoded acquisition project/store/broker
origin before I/O, and uses the authority's broker epoch and invocation identity. Production `Command`
retains or reconstructs one exact plan through rendering, snapshot persistence/binding, root-refined
lifecycle-context admission, hidden fixed root-Up entry interpretation, and current-frame teardown. The entry
producer alone derives journal/cursor admission, invokes generic `authorizeRootProject`, and reaches public
Chain. The Production
compatibility authority, forward-interpreter, descriptor, and teardown-plan APIs are absent. Harness
constructs its exact generative-run plan after variant selection and retains it through generated-config
ownership, the same fixed root-Up entry, and the matching reverse projection.
Plan-owned resources and edges, the broker-indexed acquisition journal, same-broker
per-frame cursor, generic root-refined command authority, exact plan-projected descriptor, and exact
pure frame-indexed reverse projection are implemented. The recovered profile and exact fixed-identity
reconstruction are also implemented.

Sprint 12.21 splits journal ownership without creating another public admission route.
`HostBootstrap.Lifecycle.Mode.withAcquisitionJournal` owns retained evidence checks, rereads the live
mode, exact lease version/state/bytes, and canonical snapshot in the lease's one protected-store entry,
then invokes the callback after that entry closes. `HostBootstrap.Lifecycle.Session` owns the separate
`acquisition.<project>.<run>.<brokerEpoch>` record and its strict binding/phase codec. That record is
neither the Phase-10 `project.<planDigest>` Open/Closing transaction nor a resource-operation attempt
record.

Sprints 12.22–12.23 keep one facade split: 12.22 owns the opaque value and canonical record, while 12.23
owns admission, recovery, and transitions. `HostBootstrap.Lifecycle.Mode` publicly reexports the cursor
and its expected-phase, current-phase, and successor eliminators. `HostBootstrap.Lifecycle.Session` owns
the `cursor.<sha256>` key derived from canonical length-framed acquisition-key/frame bytes and the strict
canonical payload binding the exact source key/version/bytes, frame, immutable verb, and current phase.
The acquisition phase seeds a frame only until its cursor row exists; thereafter that row is
authoritative. Distinct frames therefore advance independently. Exact retries and concurrent readers may
redeliver the current cursor, but only one CAS can reserve each successor.

The plan-owned resource/edge projections and atomic local command-reservation foundation are
implemented. The specialized root gate
`HostBootstrap.Authority.ProjectPlan.authorizeRootProject` consumes the complete retained bound-plan
package, typed cursor, root-refined lifecycle context, and the lease's private protected origin before the
narrow Session bridge enters one protected transaction for live mode/lease/snapshot/source/cursor
revalidation plus one-use reservation. The hidden root-Up entry owns the ordinary consumer; the lower
`HostBootstrap.Authority` facade remains producer-free.

The target lifecycle algebra is shared, not reimplemented by provider/demo code:

```text
opaque resource identity
  -> total ProbeResult
  -> generative Ready lifecycle-scope plan resource-instance dependency
  -> plan-internal complete-edge traversal + fresh OperationDependencySnapshot
  -> plan-owned closed OperationPreconditionSet (exact zero/one/many edges + probes + call digest)
  -> protected prepare revalidation
  -> matching PreparedOperation + PreparedPreconditions
  -> lifecycle-scope-, plan-, ownership-, and phase-indexed conditional backend transition
  -> Either ReconcileError
       (ReconcileResult scope planId id resource Observed targetPhase)

ReconcileResult
  = ManagedResult
      (opaque ManagedTransition binding, as one value:
         ResourceHandle ... Managed targetPhase
         + OwnershipReceipt ... id resource
         + ManagedOutcome ... Observed targetPhase)
  | ForeignResult
      (ResourceHandle ... Unmanaged targetPhase)
      (Observation ... id resource)

ReconcileError
  = Conflict ConflictReason
  | SafetyRefusal RefusalReason
  | Unsupported UnsupportedReason
  | Failure FailureContext RecoveryDisposition
```

A managed unchanged result preserves teardown authority. A foreign result grants an `Unmanaged` handle
that cannot be passed to mutation or teardown; explicit adoption requires matching opaque authority and
returns a managed handle plus receipt. Recursive teardown consumes only receipts acquired by that run.
The generative `planId` also prevents two Production plans from exchanging handles, journals, or
receipts. `Down` and `Destroy` have distinct teardown-plan types; the durable root remains in the plan
under `Preserve`.
Retained `Ready` values never enter a backend adapter. Prepare reruns the plan-owned probes and
identity/version checks; only the jointly returned prepared pair can call the effect, and a backend that
cannot condition the call on that prepared version is `Unsupported`.
Ordinary project teardown preserves it in both scopes. A Harness run that acquired resources must finish
exact destroy and combine its `DestroySettled`, closed-session proof, and exact bound lease into
`ProjectClosureEvidence (Harness projectId runId)` before close authorization; only a separately verified
true pre-effect refusal may use the no-resource short close. Generated config and `.test_data/<runId>` remain
owned until that close state is settled or safely recovered.

The target execution profile is opaque:

```text
LifecycleProfile (Production projectId)
LifecycleProfile (Harness projectId runId)
RecoveredProductionLifecycleProfile projectId specDigest planDigest planId brokerGeneration
```

Existing-snapshot admission generates the one local `planId` carried by that recovered profile; recovery
opening and reconstruction retain it and do not quantify a second identity. The local command substrate
is broker-indexed end to end; its acquisition journal, frame-local lifecycle cursor, and exact `project up`
command authority are implemented:

```text
AcquisitionJournal scope planId brokerGeneration
LifecycleCursor scope planId frame brokerGeneration verb phase
CommandAuthority scope planId frame brokerGeneration verb phase
```

[The installed-identity-and-authority-kernels phase](phase-5-operator-root-and-command-authority.md) supplies
only the lower exact-root and one-use reservation kernels. [The lifecycle-modes-and-run-leases
phase](phase-9-lifecycle-modes-and-run-leases.md) owns fresh Production/Harness profile opening from the exact
root, mode, and still-unbound lease, while [the step-algebra-and-project-plan
phase](phase-12-step-algebra-and-project-plan.md) and [the recovery-and-migration
phase](phase-18-recovery-and-migration.md) own the exact bound-plan and recovered-profile paths. Exclusive
harness ownership can consume only its run-indexed fresh profile; Harness/teardown recovery cannot inhabit the
Production recovery type. Phase 12 owns the exact local bound-plan/current-frame substrate; Phase 17 alone
composes the root plan catalog, coordinator, storeless executor, and recursive traversal. The exact local root
verb/frame/phase authority derives from the lower kernels, validated plan/context, bound lease, acquisition
journal, and same-broker cursor.
The current demo `containerPlan` still derives cluster name, data root, ports, and ownership identity from an
independently passed `ClusterProfile`; Sprint 24.4 replaces that consumer boundary with one projection from the
retained exact plan. The five-field `TestSuite` is assertion-only; the Harness command retains and interprets
the exact Harness `ProjectPlan` instead of exposing a lifecycle callback or re-entering Production. Its opaque
lifecycle capability is constructed only across the private component boundary, while exact settled-destroy
closure evidence gates the ordinary terminal close.
Successful Production `ProjectUp`/`ProjectDown` closes only its terminally acknowledged
`BoundRunLease`/broker invocation; Production mode, active snapshot/revision, Open-project state, and
resource records remain. Destroy/true-pre-effect project closure is the separate mode-release path.

## Ensure Reconcilers

| Reconciler family | Host applicability | Notes |
|-------------------|--------------------|-------|
| Docker | supported host substrates | Post-binary dependency |
| Homebrew / GHC | Apple Silicon | Core reconcilers are Apple-only; Linux/guest toolchain bootstrap follows the separate bootstrap/lift path |
| Lima | Apple Silicon | Config-free provider prerequisite reconciler; direct Colima is a separate exact plan-bound wall owned by [the cluster-lifecycle, budgets, and cordoning phase](phase-16-cluster-lifecycle-and-cordoning.md) |
| Incus | Apple/Linux | Apple Incus is explicit-provider support; demo default uses Lima |
| WSL2 | Windows | Provider install/readiness and the consolidated provisioning route come from [the host-providers phase](phase-15-host-providers-and-the-lift.md); native confirmation belongs to [the Windows/WSL2 acceptance phase](phase-27-windows-and-wsl2-substrate.md) |
| CUDA | Linux GPU | Requires detected NVIDIA device/driver visibility |
| CUDA Windows | Windows GPU | Host-native build stack |
| Apple Metal | Apple Silicon | Host-native accelerator build stack |

Reconcilers adopt [the canonical-quantities-and-reconcile-results phase](phase-6-canonical-quantities-and-reconcile-results.md)'s `ReconcileResult` contract. A mere executable-present Boolean is not
the final reconciler state model.

## Project Configuration

Each built project binary owns a sibling `<project>.dhall`. The current config type is project-defined
through identity-parametric `ProjectSpec cfg tcfg`, where `cfg` is scope-indexed; core does not own
universal project defaults. The static value has no runtime scope/specification/plan phantom; scope
finalization produces exactly `FinalizedProjectSpec scope specDigest cfg`. One restricted assembler quantifies installed identity and supplies Production
and Harness configs, a `ServiceRegistry cfg` projects at either selected scope, and matching mapped codecs
admit their distinct wire schemas. The CLI verifies the executable identity once and retains it through
every dispatch boundary. Context fields describe placement and requested roles but do not themselves mint
mutation authority.

Current boundaries and open adoption:

- `BinaryContext`, `Capability`, and `RuntimeWitness` are constructible descriptive records and authorize
  nothing. [The step-algebra-and-project-plan phase](phase-12-step-algebra-and-project-plan.md) now
  implements the pure, jointly generated plan-bound `CurrentFrame`, `ProjectFrame`, and `ValidatedContext`
  package; the hidden root-Up entry consumes that package and alone supplies public Chain's exact execute-phase evidence;
- `addRole` admits only the closed compatible leaf-to-leaf relation, and `validateTopology` rejects duplicate,
  cyclic, disconnected, illegal-placement, and incomplete witness graphs. These descriptive checks belong to
  [the Dhall-configuration-and-generic-project-model phase](phase-7-dhall-configuration-and-project-model.md);
- project commands read and admit one sibling `ValidatedConfig`. Production retains or reconstructs one
  opaque `ProjectPlan` and carries it through rendering, persistence, the hidden root-Up entry, lower Chain,
  and current-frame teardown. Harness constructs one exact
  `ProjectPlan (Harness projectId runId) ...` per selected variant and retains it through generated-config
  ownership, the same hidden entry/lower Chain interpreter, and exact reverse;
  service dispatch likewise verifies one snapshot and closes its action over the request. [The service-runtime
  phase](phase-22-service-runtime.md) owns changing the matching
  `ValidatedServiceRequest specDigest configId secretDigest fields service`/
  `RoleParams specDigest configId secretDigest fields service` handler from raw `IO` to a closed
  `ServiceProgram`;
- role selection is typed, and role wires contain framework validation plus only selected service fields while
  the full generated service/daemon config retains unrelated plan fields. Effect-indexed authorization and
  one-use execution remain [the service-runtime phase](phase-22-service-runtime.md)'s work;
- typed `CaseId`/`VariantId`, the total `TestMatrix` relation, and the demo's projection of declared
  `testVariants` from `<project>.test.dhall` are implemented by [the test-harness-and-run-ownership
  phase](phase-19-test-harness-and-run-ownership.md) and [the worked-demo phase](phase-24-worked-demo.md);
- `SecretRef scope` is opaque; Production cannot represent `TestPlaintext`, and Harness plaintext requires the
  matching generative run authority. [The authenticated-handoff phase](phase-13-authenticated-handoff-and-child-admission.md)
  supplies scope-first root-authenticated wire and exact signed grants, while [the recursive-lifecycle-command
  phase](phase-17-recursive-lifecycle-command.md) owns their storeless executor adoption; and
- exact authority-indexed Production/Harness profile construction is implemented by [the lifecycle-modes-and-run-leases
  phase](phase-9-lifecycle-modes-and-run-leases.md). [The cluster-lifecycle-and-cordoning
  phase](phase-16-cluster-lifecycle-and-cordoning.md) supplies backend operations and receipts; [the
  installed-identity-and-authority-kernels phase](phase-5-operator-root-and-command-authority.md) supplies only
  the lower root and reservation kernels; [the step-algebra-and-project-plan
  phase](phase-12-step-algebra-and-project-plan.md) owns the exact local/current-frame plan, journal, cursor,
  and root authority substrate; and the [recursive-lifecycle-command
  phase](phase-17-recursive-lifecycle-command.md) alone owns the root coordinator, catalog, storeless
  executors, and recursive traversal.

The target test configuration uses validated `CaseId` and `VariantId` values and a project-owned typed
projection from `tcfg` to labeled `cfg` variants. `all` is a parser selector over registered case IDs, not
stored configuration.

The target secret boundary uses `SecretRef scope` and a project-owned `ProjectConfig scope` (or equivalent
`cfg :: Type -> Type`, consumed as `cfg scope`). `SecretRef (Production projectId)` exposes only `Vault`,
`TransitKey`, and `Prompt`;
`TestPlaintext` requires matching `HarnessConfigAuthority projectId runId` and constructs only
`SecretRef (Harness projectId runId)`. Ordinary init/decode/dispatch remains project-indexed Production;
pure `psTestMatrix` validates stable variant drafts, then a fresh per-variant
`HarnessAuthority projectId runId` enters the shared `psAssemble`. Separate Dhall
schemas reject test plaintext on the production path before mutation. Harness Dhall decodes to an
untrusted wire type. The existing Offer/Relay/Receiver path now carries the Handoff facade's signed scope
capsule before received bytes introduce the scope phantom: the root link mints it, nested links copy only its
exact canonical bytes, and Receiver verifies it against the independently installed identity and key before
key, binding, challenge/grant, or payload semantics. Exact-byte config verification separately checks
`VerifiedConfigWire` and `ValidatedConfig` against the rooted binding's separately framed
child-configuration digest claim, which equals its complete-payload digest for config kind.
Only
`Config.Schema.withVerifiedConfigHandoff` joins those values into the fully indexed config-handoff proof
that `ProjectPlan.Construct.withChildProjectPlan` accepts as plan evidence. Controller restarts use a separately signed,
config-digest-bound runtime manifest rather than replaying that edge handoff. Raw wire cannot be promoted
merely because run authority exists, and no exported coercion can widen harness config into production
or another project/run.

## Thin Python Bootstrapper

| Surface | State | Contract / open work |
|---------|-------|----------------------|
| `doctor` | Implemented | Report the irreducible pre-binary host floor |
| `build` | Implemented | Explicit Cabal-file selection, one validated package/executable/artifact identity, conditional index refresh, explicit offline refusal, and unchanged-copy no-op |
| `run` | Implemented | The same idempotent host-native build followed by POSIX `exec` or a Windows child subprocess |
| `update` | Implemented | Explicit operator-invoked pipx self-update |
| `base build` | Implemented | Verified repository authority, native request/host/engine architecture equality, and the complete Python/core/demo source gate precede local inspection builds |
| `base build-and-push` | Implemented | Current-compatible resolution → source gate → native build → push rolling tag → pull → real-demo compatibility smoke; a digest may identify the pulled build without locking inputs |
| `check-code` / `test-all` | Implemented | Exposed only from the canonical checkout's in-project Poetry development interpreter through opaque maintainer authority |

Python does not own project Dhall, Docker/provider ensure, project-container construction, lifecycle, or
runtime cordons.

## Base Image and Warm Store

The rolling base image contains the Haskell toolchain, build tools, Kubernetes/container tools, and a
broad best-effort Cabal warm store selected from current compatible upstream versions at build time. It
contains no project binary and exposes no freeze-only integration `LABEL`/`ENTRYPOINT`. Projects
integrate by Cabal dependency plus `runHostBootstrapCLI`, use the same `cabal.project` on the host and in
a derived container, and may resolve/download/compile dependencies on a cache miss.

Current contracts:

- [the worked-demo phase](phase-24-worked-demo.md) makes every derived demo build pull the published rolling
  base and makes the host-native lane consume its freshly resolved repository digest;
- [the base-image-and-warm-store phase](phase-23-base-image-and-warm-store.md) supplies one host-compatible
  consumer project and opportunistic cache reuse;
- that phase's rolling build-time discovery selects current compatible releases over TLS and retains available
  integrity checks without becoming a committed replay lock; and
- its documented vanilla/dynamic shared-library ways match the artifacts present; profiling remains off unless
  explicitly enabled and validated.

## Command Tree

The supported top-level project-binary tree is:

```text
project init|up|down|destroy
test init [--replace]
test run <case-id>|all
service init|schema|run
context ...
check-code
```

`test init` writes through an opaque `TestConfigWrite` whose only producer resolves the sibling destination
from the project's own name, so the command supplies a typed test-config value rather than a path or rendered
bytes. Its replacement policy is stated rather than defaulted: an existing `<project>.test.dhall` is refused
by name unless `--replace` is passed.

[The test-and-context-commands phase](phase-20-test-and-context-commands.md) owns exact parser/gate
reconciliation, including which `context` operations exist and which commands may run without a sibling
config. Its static contract is implemented and its declared live linux-cpu sequence remains open. No
project-appended verbs or standalone `ensure` command are part of the target tree.

## hostbootstrap-demo

The demo is the worked consumer. Its current code includes VM/direct provider paths, kind/nvkind,
MinIO-backed registry storage, a web SPA, service ConfigMaps, and accelerator worker/daemon paths.

Current demo contracts:

- [the worked-demo phase](phase-24-worked-demo.md) owns the finalized proxy-through-registry topology and the
  repeated push/pull plus registry-pod-persistence proof;
- the demo's five-field `TestSuite` is assertion-only. Core retains one exact Harness-scoped `ProjectPlan`
  through generated-config ownership and the shared forward/reverse interpreter. Cluster, durable-root, port,
  and ownership identity currently still pass through config-derived `RunProfile`/`ClusterProfile` terms;
  Sprint 24.4 replaces those independent consumer inputs with the retained plan projection. The configured
  durable-readback case remains an explicit failing assertion until Phase 17 supplies a safe same-run
  destroy/restart transition and Phase 24 wires the write → destroy → fresh up → read proof;
- every derived compatibility build pulls the published rolling base, with a freshly resolved repository
  digest allowed only as a within-run identity rather than a consumer lock;
- typed cases and config variants derive from decoded test config, and the static demo component participates in
  the canonical `cabal test all` gate;
- Sprint 24.5 adds the sole closed `ProviderResourceDeclaration` step extension and corresponding plan
  projection so provider identity binds to the declared current or unique descended target frame, then requires
  exactly one such provider at a cluster frame's topology-derived direct parent. The current literal
  `ProviderResourceKind` → `core:deploy-vm`/executing-frame shortcut cannot satisfy that contract;
- Sprints 24.6–24.7 seed the neutral exact-plan execution package and project the concrete demo workload,
  partition, and exact slices. Sprints 24.8–24.18 own the separate invocation-local canonical/live dependency
  registries, provider recovery, VM/Direct/share/alias adopters, and authenticated parent-serviced reprobe;
- Sprints 24.19–24.23 render exact Kind/nvkind config and loopback publication, bind the plan-owned driver/config,
  resolve the closed backend, recover fresh cluster readiness, and adopt reconcile/cordon/readiness;
- Sprints 24.24–24.27 own the planned chart resource, prepared/settled backend, readiness/activation-gated chart
  adopter, and exact demo reverse command. Active Sprint 24.28 retains the frozen partial demo projector, Sprint
  24.29 owns authenticated derived-image adoption, and Sprint 24.30 alone owns live acceptance; and
- named non-baseline accelerator and durability confirmation belongs to [the Apple Silicon](phase-25-apple-silicon-substrate.md),
  [NVIDIA GPU](phase-26-nvidia-gpu-substrate.md), and [Windows/WSL2](phase-27-windows-and-wsl2-substrate.md)
  acceptance phases.

[The worked-demo phase](phase-24-worked-demo.md) owns demo wiring, provenance, and config-driven variants.
Reusable configuration, plan, and harness contracts belong respectively to [the Dhall-configuration-and-generic-project-model
phase](phase-7-dhall-configuration-and-project-model.md), [the step-algebra-and-project-plan
phase](phase-12-step-algebra-and-project-plan.md), and [the test-harness-and-run-ownership
phase](phase-19-test-harness-and-run-ownership.md).

## Update Rule

When a component changes, update this inventory's state and purpose, the owning phase, and the governed
canonical documentation together. Do not add a phase status or test-count roll-up here — the
[README phase table](README.md) is the sole status authority (§ J).

Each row names its owning phase by **name**, not by number, so a renumbering does not falsify this file
(§ A, § J).
