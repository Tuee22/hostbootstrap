# Phase 15: Binary context config and command gating

**Status**: Authoritative source
**Supersedes**: N/A
**Referenced by**: [README.md](README.md), [00-overview.md](00-overview.md), [system-components.md](system-components.md), [binary_context_config](../documents/architecture/binary_context_config.md)

> **Purpose**: Make every project binary explicitly know where it is in the composed
> host/VM/container/cluster chain through a sibling `<project>.dhall`, make existing-frame commands fail
> fast when that descriptive config is missing or incompatible, and separate target opaque command
> authority from config data.

## Phase Status

**Status**: Active

**Reopened and closed (2026-07-02)** for **in-place child-config delivery** (development_plan_standards
§ U, § X): the chain retains a named `context-init` frame anchor, while the current VM projection is
produced inside the composite bootstrap action and the container projection is supplied independently
through `psFrameContext` at the lift boundary. The context-adjusted full-record projection is **streamed
into the child frame over the lift's `stdin` channel** and written by the descending binary to its own sibling
`<project>.dhall` before dispatch — no **parent-side intermediate** config file and no config bind-mount
for the VM and project-container frames (the Kubernetes service pod keeps its ConfigMap override). The
wire is absent from `argv`; after receipt, the child sibling file is intentionally persisted and can be
decoded/inspected. Current descriptive context/capability declarations are narrower, but the demo still
sends its full parameter record and parent resource envelope; these bytes are not opaque authority.
**Closed 2026-07-02**: the core `Lift.ConfigDelivery` +
`Chain` stdin-handoff landed (`cabal build all --ghc-options=-Werror` and `cabal test all` green — 280
tests, incl. new pure `LiftSpec` cases proving the projection rides `stdin` and is absent from `argv`), and
a live Windows/WSL2 `test run all` reported **`6/6`** with the VM config streamed in-place, the container
config streamed on the handoff `stdin`, **no** `.vm.dhall`/`.runtime-container.dhall` produced, and **no**
config bind-mount. See Sprint 15.7 and
[legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md); the demo surfaces closed in
[phase-13](phase-13-hostbootstrap-demo.md) Sprint 13.15.

**Reopened (2026-06-19) and closed (2026-06-20)** for the explicit-context / multi-role /
forwarded-parameter refinements: each `<project>.dhall` carries an explicit context; a config may declare
**more than one role**, generated by `project init --role … --also-role …`
(`HostBootstrap.Context.addRole` unions the added role's command classes + capabilities without changing
the primary kind); child configs are generated from parameters forwarded from the parent (the
recursive handoff projection plus the ConfigMap override for a service pod); and the read-only `context` command is
uniform over all config shapes. The union mechanism landed, but it is not proof that every widened
combination is executable: `service run` rejects a non-leaf primary kind, while all three current project
lifecycle gates (`up`, `down`, and `destroy`) are class-based and can incorrectly accept an
orchestration-widened leaf. Sprint 15.9 owns that open authority gap. See `## Remaining Work`.

Phases 6, 8, 11, 13, and 14 provide the needed substrate: the thin Python bootstrapper, typed Dhall
generation, the self-reference lift, the worked demo chain, and the composition methodology. This phase
turns the implicit context carried by lift composition into an explicit runtime precondition for every
binary process.

The runtime context is folded into the project-local `<project>.dhall` file. Existing-frame commands gate
through the sibling project config, context creation surfaces materialize host/VM/container/service
placement configs, and cluster lifecycle commands use the active context as their current descriptive
input. Config-free writers, static inspection, decode-only inspection, and the harness-managed test-config
path are deliberate exceptions listed below. The context schema, loader, command gate, topology frames,
and runtime witnesses are built and validated; the Python boundary holds.

This phase was **realigned** to the "chain is the project" model (§ Y, § Z, § X), and the binary-context
contract now matches it: the sibling `<project>.dhall` carries **parameters + context + witness** and never
the chain shape; child-config creation is off the dissolved `context create` mutation verb. Current
`project up` projection/delivery is split across child-projection functions, composite bootstrap or
`psFrameContext`, and the lift; the named `context-init` action is only an announcing anchor. Sprint 16.6
owns their unification in one plan operation. The host-level default surface is renamed from `config init` to
`project init`, which is explicit and is not triggered by Python; the read-only `context` command
(`renderComposition` + `context inspect`)
absorbs the former `config schema` / `config show FILE` / `config path` / static `config render` surfaces;
and the per-frame fail-fast mechanism validates the selected frame's ancestry and each supplied witness.
It does not require a provider/kind-complete witness set or validate the whole graph for duplicates,
cycles, disconnected frames, `parentChain` disagreement, or illegal kind/provider relations. The
schema/loader/gate substrate (Sprints 15.1, 15.2, 15.6) and the realigned command-surface
sprints (15.3, 15.4, 15.5) are `Done`; the recursive per-frame gate is implemented, while Sprint 16.6
owns making the child projection/handoff action and lifecycle row one typed operation.

**Reopened 2026-07-24 for opaque role-specific command authority and handoff transport.** The current
gate infers permission from forgeable config classes/capabilities, `addRole` can create incoherent
kind/class combinations, and the config-only stdin handoff carries no one-time run authority. Sprint
15.9 replaces that model with command-specific opaque authority and a length-delimited
config+`HandoffToken` envelope issued only after an `UnboundRunLease` has been verified/bound to the
exact plan digest as `BoundRunLease`, with atomic nonce consumption coordinated with
Sprints 10.9 and 16.6.

**Reopened 2026-07-09 for accelerator daemon contexts.** The accelerator daemon generalization needs a
daemon-authority context that works both in-cluster and host-native, plus a direct host-backed
project-container topology for the Linux GPU `nvkind` path.

The static context slice is implemented and validated: the standard `HostBootstrap.Context` daemon
constructors give their generated contexts leaf `service run` / daemon classes without project lifecycle
classes, distinguish host-resident and in-cluster daemon projections, and expose an explicit Linux GPU
direct project-container context that requires a direct-topology witness while preserving the normal
VM-ancestor rule for VM-backed runtime containers. `ContextSpec` covers daemon authorization,
wrong-frame daemon witness failure, the Linux GPU direct topology, and rejection of an unmarked
host-backed project container. Unchecked `addRole` can still widen those generated records; Sprint 15.9
owns that separate authority defect.

The exact implemented config-input matrix is:

| Surface | Current config behavior |
|---|---|
| Help | Config-free |
| `project init`, `service init`, `test init` | Config-free writers |
| `service schema`, `context path`, `context schema`, `context render` | Static and config-free |
| `context inspect` | Reads the executable-sibling `<project>.dhall`; decode-only, without a command-class gate |
| `context show [FILE]` | Reads the selected/default file; decode-only, without a command-class gate |
| `test run <case-id>\|all` | Reads `<project>.test.dhall`, refuses an existing sibling project config, writes each variant behind the cooperative sidecar lock, and removes it only when its bytes still match; Phase 10.9 owns the four § EE ownership clauses and verified receipts, Sprint 15.9 owns opaque root/command authority, and Sprint 17.4 makes this parser route require it |
| `project up\|down\|destroy`, `service run`, `check-code` | Read the sibling project config and apply an existing-frame gate |

The default `project init` mode writes a fresh sibling host-orchestrator root and refuses an existing
output. Its current parser also accepts `--role`, repeatable `--also-role`, `--output`, `--force`, and
`--if-missing`; Phase 17 Sprint 17.4 owns the target opaque role-specific request types and overwrite
policy.

## Remaining Work

**Capability/context authority — open (Sprint 15.9).** Make capabilities and role-specific command
authorities opaque, mint them only through validated lifecycle transitions, replace unchecked `addRole`
widening with compatibility-enforcing smart constructors, narrow child authority, and require the durable
capability at its actual mutation sites. A command gate must consume handed-off authority; it must never
infer authority from descriptive Dhall fields. The same sprint owns total topology-graph validation,
mandatory provider/kind witness sets, exact root gating for every project lifecycle verb, and one
immutable validated config snapshot threaded into plan steps and typed service-request selection rather
than later sibling-file reloads. Phase 18.6 ensures only the resulting role parameters, not the full
config, reach a service handler. The cross-frame transport target is a length-delimited
private duplex broker session carrying the narrowed config wire plus an opaque `HandoffToken` issued
under the plan-digest-bound `BoundRunLease` (never the pre-plan `UnboundRunLease`). The binary receiver
returns a fresh challenge; the broker atomically consumes
the nonce and authenticates an edge-/config-hash-bound grant before dispatch. Recorded transcripts,
broker loss, and replay fail; each later invocation receives a fresh token. Authority is never serialized
into Dhall or passed through `argv`/environment. This work coordinates with Sprints 10.9 and 16.6 for run
ownership and recursive lifecycle state.

**Accelerator daemon context work — implementation complete; live integration open.**

- Done statically: daemon role/class declarations, host-resident and in-cluster daemon projections, the explicit
  Linux GPU direct project-container context, strict VM-backed runtime-container ancestry, and the
  pure/witness tests.
- Done statically: the demo renders the web-service and daemon ConfigMaps dynamically from the validated
  parent-derived projections and applies them from `HostBootstrapDemo.Commands`; the chart retains only
  the Deployment mount, and a hash of the exact config bytes rolls a subPath-mounted pod when config
  changes.
- Done statically: Phase 16 places and rollout-waits the Linux CPU/GPU daemon Deployments, while the
  Apple/Windows host path writes the host-daemon projection beside the host-native daemon binary. Phase 18
  dispatches the config-selected service variant through the concrete WebSocket runtime.
- Remaining (real-run-gated, § C): prove on the native substrate lanes that every daemon reads the
  delivered projection and connects through the intended ingress. No context implementation or static-test
  work remains.

**`Capability.DurableStore` is inconsistently and self-assertedly required — open.**
`capabilitiesForKind` grants `DurableStore` to the `ClusterService` and `Daemon` context kinds. Initial
core `service run` dispatch passes an empty requirement list, as does the accelerator handler's second
config load; only the Web handler's post-selection sibling-config reload passes `[DurableStore]` to
`validateContext`. That late membership check consumes an editable decoded declaration, not opaque
placement authority, and selection and execution can observe different files. The capability is
therefore **partial and descriptive, not enforcing**, and must not be read as backing any `.data`
durability guarantee — see
[durable_state](../documents/architecture/durable_state.md) for what the invariant does and does not say.
Promoting it to a real gate is sequenced after
[phase-5](phase-5-cluster-lifecycle-and-resource-cordoning.md) Sprint 5.6 lands a durable-state surface,
and is tracked in [legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md) `Pending`. Gate: a
command gating on durable placement refuses before dispatch unless it carries the matching opaque,
verified capability; a decoded declaration is not sufficient.

[Phase 19](phase-19-generic-project-model.md) builds **forward** on this surface (the generic project
model, § BB): the binary-context coupling becomes the generic `cfg -> BinaryContext` accessor on
`ProjectSpec projectId cfg tcfg`, with `cfg :: Type -> Type`, so the gate is expressed over a
project-defined scope-indexed config family. The superseded
fixed-`ProjectConfig` coupling is recorded in
[legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md) with phase 19 as owner. The phase-19
genericization did **not** reopen this phase; the **2026-07-02 in-place-delivery reopening** (Sprint 15.7
below) did so at the time and refined child-config *delivery* only. The current independent reopening is
Sprint 15.9's opaque capability/authority work.

Refine the binary-context contract to the explicit-context / multi-role / forwarded-parameter model
(development_plan_standards § X).

**Landed in code (2026-06-19), code-check-validated:**

- The **service-role config** is generated and gated: `service init` writes a `cluster-service`-role
  `<project>.dhall`, and `service run` gates as `Context.ServiceCommand` (the leaf-frame service-role gate)
  plus an explicit `ClusterService`/`Daemon` primary-kind check. A normal host-orchestrator config refuses
  it (verified on the real binary), but the current command passes an empty required-capability list:
  `allowedCommandClasses` membership and leaf kind are not proof of a precise service capability, and
  decoded/additional roles remain forgeable. Sprints 15.9/18.6 own typed authority
  ([phase-18](phase-18-service-runtime-command.md)).
- A context's relationship to the others remains expressed in the **pure compositional lifts** (the
  `topologyFrames` / `parentChain` frame graph), surfaced read-only by `context inspect`.
- Child configs are generated by the binary from passed parameters, some forwarded from the parent.
  Current VM/container projection and lift delivery are separate from the announcing `context-init` row;
  a service pod uses the ConfigMap overriding the baked container config (§ AA).
- The read-only `context` command treats **all** `<project>.dhall`s uniformly, regardless of the roles a
  config declares.

**Multi-role union generation landed (2026-06-20), code-check-validated** (`cabal test all` green, 226
tests; verified on the real binary): `HostBootstrap.Context.addRole` unions a secondary role's command
classes + capabilities into a context while leaving the primary kind/frame unchanged, and the shared init
parser exposes repeatable `--also-role ROLE`. That is the accurate scope of the landing. A
`host-orchestrator` primary widened with the service role carries `ServiceCommand`, but `service run`
still rejects it because its explicit kind gate accepts only `ClusterService` or `Daemon`. In the other
direction, `project up` performs only the `ClusterLifecycleCommand` class gate, so a `Daemon` or
`ImageBuildContainer` primary widened with an orchestration role can incorrectly begin orchestration.
`ContextSpec` covers the raw union and `CLISpec` covers the non-leaf service refusal; Sprint 15.9 must
close the project-up widening hole and replace raw role/class combinations with opaque compatible
authority. The contract is documented in
[binary_context_config.md](../documents/architecture/binary_context_config.md). The service-role config is
consumed by [phase-18](phase-18-service-runtime-command.md), whose base service command is delivered and
whose accelerator reopening remains `Active` for live integration.

The realigned contract built earlier remains valid and validated:

- `context` is a **read-only composition-introspection** command (`HostBootstrap.Context.renderComposition`
  + `context inspect`, `ContextSpec`), absorbing the former `config schema` / `config show FILE` /
  `config path` / static `config render` surfaces; no mutation. Its inputs are not uniform:
  `inspect` reads the sibling config, `show` reads its selected/default file, and
  `path`/`schema`/`render` are static and config-free.
- The standalone `context create vm|container|service` verb is removed. Child projection and delivery are
  internal `project up` work, but are currently split from the announcing `context-init` step kind.
  Sprint 16.6 owns the single plan node that must bind them.
- The host-level default-config surface is the explicit `project init` (§ Y). The Python bootstrapper
  builds and invokes the requested command without initializing config (POSIX `exec`; Windows child
  subprocess).
- The **per-frame fail-fast mechanism** is `validateRuntimeContext` over the declared topology witnesses,
  built and unit-tested; the recursive-handoff gating of `project up` is owned by phase-16.
- `.dhall` = **parameters + context + witness**, never the chain shape; structural variation is a
  root-`<project>.dhall` flag, so the chain stays a pure function of root parameters (§ Y).

The `project` lifecycle command and recursive interpreter that carries the per-frame fail-fast handoff
are owned by phase 16. Its Sprint 16.6 also owns replacing the independent child-projection/delivery paths
and announcing `context-init` row with one plan operation; this phase owns the binary-context and command-
authority contract those changes rest on.

## Phase Objective

Introduce the runtime binary-context contract:

- Existing-frame commands (`project up|down|destroy`, `service run`, and `check-code`) read
  `<project>.dhall` from next to the executable before dispatch.
- The init writers are config-free; `service schema` and `context path|schema|render` are static and
  config-free; `context inspect` reads the sibling, `context show [FILE]` reads its selected/default file,
  and `test run` reads the test config, refuses a sibling project config, and generates its own.
- The project name in the filename is derived from the Cabal file/binary identity; the role is inside the
  file content.
- The built project binary creates project-owned defaults through explicit `project init`; its current
  `--role` / repeatable `--also-role` / `--output` / `--force` / `--if-missing` flags retain a fresh-root,
  refuse-existing default pending Sprint 17.4's opaque init-request and overwrite-policy repair.
- Each nested boundary receives or creates its own role-specific local config before the nested binary
  runs.
- The Dockerfile path materializes the declared image-build context through the project-owned
  `project init` family after installing the binary and before `check-code`; runtime containers receive
  parent-generated configs for actual topology frames.
- Kubernetes service pods receive a service/daemon local config from their owning controller; durable
  services use a `StatefulSet`.
- Missing, undecodable, wrong-project, wrong-capability, or wrong-command contexts fail fast with exit
  code 1.
- A supplied malformed or false topology witness fails fast before side effects. An empty/incomplete
  witness list can currently pass because no provider/kind-specific required set is enforced; Sprint
  15.9 closes that relation.

## Sprints

### Sprint 15.1: Context schema, loader, and command gate [Done]

**Status**: Done
**Implementation**: `core/hostbootstrap-core/src/HostBootstrap/Context.hs`, `core/hostbootstrap-core/test/ContextSpec.hs`, `core/hostbootstrap-core/hostbootstrap-core.cabal`
**Docs to update**: `documents/architecture/binary_context_config.md`, `documents/engineering/dhall_topology.md`, `documents/architecture/hostbootstrap_core_library.md`

#### Objective

Define the runtime context fields inside `<project>.dhall` and the Haskell substrate that loads the
sibling config before existing-frame optparse dispatch.

#### Deliverables

- `HostBootstrap.Context`: context type, Dhall decoder/encoder, sibling-file discovery, and validation.
- Current command-gating API checks project/binary identity, declared local capabilities/command classes,
  the selected frame's ancestry, and each supplied witness before invoking existing-frame handlers.
  `service run` adds a primary-kind check. It does not validate the entire topology, require a complete
  witness set, or root-gate `project up|down|destroy`; Sprint 15.9 owns those repairs.
- Exit-code-1 fail-fast behavior with one-line diagnostics for missing or invalid contexts.

#### Validation

- Unit tests for successful context load, missing file, decode failure, project mismatch, unavailable
  capability, and command/context mismatch.
- CLI tests proving existing-frame commands fail before side effects when the context is absent or
  invalid.

#### Remaining Work

None. `cabal test all` passes with `ContextSpec` covering render/decode, sibling path discovery, missing
and malformed context files, project/binary/command/capability mismatches, exit-code-1 fail-fast behavior,
and no-side-effect command gating.

### Sprint 15.2: Python stays outside Dhall ownership [Done]

**Status**: Done
**Implementation**: `hostbootstrap/bootstrap.py`, `tests/test_bootstrap.py`
**Docs to update**: `documents/architecture/python_haskell_boundary.md`, `documents/architecture/build_and_run_model.md`, `documents/engineering/resource_budgeting.md`

#### Objective

Keep the Python bootstrapper outside Dhall ownership. The host-native binary owns config creation; Python
builds and invokes it without creating or triggering a default local config.

#### Deliverables

- Python discovers the single Cabal file and executable stanza independently and writes no Dhall; Phase
  6.7 owns validation that all identity projections agree.
- After building `./.build/<executable>` host-native, Python invokes the requested command without
  triggering config initialization: process-replacing `exec` on POSIX, child subprocess on Windows. A
  user creates the program-name sibling config explicitly with `<executable> project init`.
- The project binary owns decoding, rendering, validation, and command gating for the local config.

#### Validation

- Python tests prove Cabal-file project discovery, zero/multiple-Cabal diagnostics, host-native
  build/handoff argv, and the absence of Python-written Dhall artifacts.

#### Remaining Work

None. Validation:
`poetry run python -m hostbootstrap.test_all -q` passes (113 tests), `poetry run python -m
hostbootstrap.check_code` passes, and `cabal test all` passes.

### Sprint 15.3: Nested context creation surfaces [Done]

**Status**: Done
**Implementation**: `core/hostbootstrap-core/src/HostBootstrap/Context.hs`, `core/hostbootstrap-core/src/HostBootstrap/Command.hs`, `core/hostbootstrap-core/src/HostBootstrap/CLI.hs`, `demo/docker/Dockerfile`, `core/hostbootstrap-core/test/ContextSpec.hs`
**Docs to update**: `documents/engineering/derived_dockerfile.md`, `documents/engineering/derived_project_standards.md`, `documents/operations/demo_runbook.md`

#### Objective

Expose project-binary entrypoints for creating role-specific project-local configs at each nested
boundary without weakening existing-frame command gating.

#### Deliverables

- Historical initial surface: `context create vm|container|service OUTPUT` derived child configs and
  `config init` materialized the image-build context. Both command forms were later removed.
- Current delivered surface: child-projection functions derive each child `<project>.dhall` from the
  active parent during `project up`, and the lift streams the selected projection. The named
  `context-init` action is only an announcing frame anchor. The project-owned `project init` family
  materializes the image-build context before `check-code`.
- VM-context creation happens before invoking the binary inside a managed VM.
- Kubernetes service-context generation/mounting guidance covers StatefulSets and other controllers.

#### Validation

- Unit tests for host -> VM and host/VM -> container context derivation.
- Dockerfile fixture or integration test proving `check-code` runs only after the container context
  exists.
- Rendering or manifest tests for StatefulSet context placement when a project defines a cluster service.

#### Remaining Work

None in this historical sprint's command-surface scope. The child-config constructors remain and the
standalone `context create` mutation verb is removed. During recursive `project up`, composite
bootstrap/`psFrameContext` code selects the context-adjusted full-record projection and the lift delivers it; the named
`context-init` action only announces. Sprint 16.6 owns unifying those independently supplied paths in one
plan node. The `context` command is read-only. The declared image-build context is materialized through
the project-owned `project init` family before `check-code`; Python does not invoke either initializer.

### Sprint 15.4: Normal runtime config reads [Done]

**Status**: Done
**Implementation**: `core/hostbootstrap-core/src/HostBootstrap/Context.hs`, `core/hostbootstrap-core/src/HostBootstrap/Command.hs`, `core/hostbootstrap-core/src/HostBootstrap/Ensure.hs`, `core/hostbootstrap-core/test/ContextSpec.hs`, `demo/src/HostBootstrapDemo/Commands.hs`
**Docs to update**: `documents/engineering/schema.md`, `documents/architecture/dhall_generation.md`, `DEVELOPMENT_PLAN/legacy-tracking-for-deletion.md`

#### Objective

Ensure existing-frame runtime dispatch uses the sibling project-local config.

#### Deliverables

- Normal command handlers execute through context-backed dispatch or an explicit inspection-only path.
- The removed `config show FILE` inspection surface is absorbed into the read-only `context` group.
- Update the demo so host, VM, container, and cluster-service copies of the binary each use their own
  context and reject non-commensurate commands.

#### Validation

- Tests proving project lifecycle, daemon/service, check-code, and host-orchestrator handlers are accepted
  only when the current descriptive gate passes. `test run` instead manages a generated sibling config
  behind the current cooperative sidecar/matching-byte guard; the § EE ownership clauses and
  verified receipts are Phase 10.9 work, opaque root/command authority is Sprint 15.9 work, and parser
  enforcement of that authority is Sprint 17.4 work.
- Demo dry-run output and unit tests proving the lifted sequence still has one representation while each
  nested process receives the expected context.
- `cabal test` and the Python `test_all` runner pass.

#### Remaining Work

None. Context-backed dispatch loads and validates the sibling config for `project up|down|destroy`,
`service run`, and `check-code`. The config-free writers are `project init`, `service init`, and
`test init`; help, `service schema`, and `context path|schema|render` are config-free;
`context inspect` and `context show` are decode-only reads; and `test run` manages the generated sibling
through the current cooperative sidecar/matching-byte cleanup guard instead of loading a production
config. Resource-authoritative reservations and verified ownership receipts remain Phase 10.9 work. The
removed `config`, `cluster`, and demo
noun-verb surfaces are tracked in
[legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md); lifecycle mutation is interpreted
from the project plan under `project up|down|destroy`. Phase 17.4 owns the target parser/request matrix,
not this completed sprint.

### Sprint 15.5: Context lives in `<project>.dhall` [Done]

**Status**: Done
**Implementation**: `core/hostbootstrap-core/src/HostBootstrap/Context.hs`,
`core/hostbootstrap-core/src/HostBootstrap/Config/Schema.hs`,
`core/hostbootstrap-core/src/HostBootstrap/Command.hs`, `core/hostbootstrap-core/src/HostBootstrap/CLI.hs`,
`core/hostbootstrap-core/src/HostBootstrap/Chain.hs`,
`core/hostbootstrap-core/src/HostBootstrap/Lift.hs`, `demo/docker/Dockerfile`,
`demo/chart/templates/deployment.yaml`, `demo/src/HostBootstrapDemo/Commands.hs` (dynamic ConfigMap
render/apply), `core/hostbootstrap-core/test/ContextSpec.hs`,
`core/hostbootstrap-core/test/SchemaSpec.hs`, `core/hostbootstrap-core/test/ChainSpec.hs`,
`core/hostbootstrap-core/test/LiftSpec.hs`
**Docs to update**: `documents/architecture/binary_context_config.md`,
`documents/engineering/schema.md`, `documents/engineering/dhall_topology.md`,
`documents/engineering/derived_dockerfile.md`, `documents/operations/demo_runbook.md`,
`legacy-tracking-for-deletion.md`

#### Objective

Use the project-local `<project>.dhall` file as the single descriptive runtime-config input for each
binary copy; opaque mutation authority is separate blocked Sprint 15.9 work.

#### Deliverables

- Sibling-file discovery looks for `<project>.dhall`, where `<project>` is derived from the running
  binary/Cabal identity.
- The context loader reads the runtime-context section inside that config and enforces the
  command/capability gate.
- Dockerfiles use the project-owned `project init` family to materialize
  `/usr/local/bin/<project>.dhall` as the declared build-time context.
- Daemon/service startup logs include project, binary, context kind, role name, config path, config hash,
  source root, and resource envelope, with secrets excluded.
- The core gate loads one config for initial dispatch. Current demo project steps and both service
  handlers later reopen the sibling file, so a replacement can mix the plan/selector from snapshot A
  with resources or handler settings from snapshot B. Sprint 15.9 threads one immutable validated
  snapshot through plan steps and service-request selection; Sprint 18.6 packages only the derived
  role-specific parameters with the handler. Daemons then require restart/reconcile to observe changes.

#### Validation

- Unit tests cover sibling `<project>.dhall` discovery, missing/malformed file failures, wrong-project
  failures, role/capability mismatches, and no-side-effect command gating.
- Dockerfile ordering installs the baked `/usr/local/bin/<project>.dhall` default before `check-code`;
  record a Docker build smoke here when that image build is run.
- Daemon logging tests verify startup metadata includes the config hash and excludes secret values.
- Python tests prove no host context file is written by the bootstrapper.
- Historical phase-close validation (superseded by the fixed command surface): `cabal test all` from
  `core/` passes (159 tests); `cabal build all` from `demo/`
  passes; `helm template hostbootstrap-demo demo/chart` renders only the service-role
  `hostbootstrap-demo.dhall` mount; `cabal run hostbootstrap-demo -- config init --role host-orchestrator
  --source-root /home/matt/hostbootstrap/demo --dockerfile docker/Dockerfile --cpu 6 --memory 10GiB
  --storage 80GiB --ha-replicas 1 --force` creates the host config;
  `cabal run hostbootstrap-demo -- config init --role image-build-container --output
  <tmp>/hostbootstrap-demo.dhall --source-root /workspace/demo --dockerfile docker/Dockerfile --cpu 6
  --memory 10GiB --storage 80GiB --ha-replicas 1 --force` writes a non-empty image-build config with
  `ImageBuildContainer`; and `cabal run hostbootstrap-demo -- deploy --dry-run` renders the single lift
  sequence through the gate.

#### Remaining Work

None in the historical sibling-discovery/descriptive-gate landing. Current membership gating and startup
metadata are built. Sprint 19.8 made service dispatch read and canonically verify one sibling snapshot;
general demo lifecycle step actions are not yet all closed over one validated config, so Sprint 15.9
owns plan-wide immutable snapshot identity/injection. Explicit
`project init` owns host config creation; Python never auto-initializes. The declared image-build context
uses the project-owned initializer before `check-code`, and `project up --dry-run` renders the project
plan through the current gate. The sibling config carries parameters + context + witness, never the plan
shape or target opaque command authority.

### Sprint 15.6: Topology frames and runtime witnesses [Done]

**Status**: Done
**Implementation**: `core/hostbootstrap-core/src/HostBootstrap/Context.hs`, `core/hostbootstrap-core/src/HostBootstrap/Config/Schema.hs`, `demo/docker/Dockerfile`, `demo/src/HostBootstrapDemo/Commands.hs`
**Docs to update**: `documents/architecture/binary_context_config.md`, `documents/engineering/dhall_topology.md`, `documents/engineering/schema.md`, `documents/operations/demo_runbook.md`, `legacy-tracking-for-deletion.md`

#### Objective

Add topology/witness data and validate the selected frame's supplied ancestry/witness facts. The
historical sprint did not establish whole-graph validity or a required witness set.

#### Deliverables

- Add an execution topology to `<project>.dhall`: frames, parent references, provider kind, current frame,
  capabilities, command classes, resource envelope, and runtime witnesses.
- Keep the frame graph open-ended so higher layers can represent arbitrary chains such as host -> Lima VM
  -> Docker container -> kind cluster -> pod or host -> VM -> Pulumi role -> EKS cluster -> workload.
- Add local witness interpreters for supplied host, Lima VM, Incus VM, Docker runtime-container, and
  Kubernetes service facts. Current validation does not require the provider/kind-complete set.
- Change Dockerfile defaults from VM-project-container context to image-build context.
- Require parent-generated runtime configs for lifted `test run all`.

#### Validation

Historical phase-close validation (superseded by in-place config delivery): `cabal test all` from `core/`
passes (199 tests); `cabal build all` from `demo/`
passes; `cabal run hostbootstrap-demo -- deploy --dry-run` renders the VM-local
`context create container` step and mounts the generated runtime config plus `/run/hostbootstrap` witness
directory into the lifted `docker run`; `ContextSpec` covers topology ancestor rejection, direct
image-build `test all` rejection, and runtime witness checking; `SchemaSpec` covers direct
host-to-runtime-container projection rejection.

#### Remaining Work

None in the historical field/supplied-witness landing. Sprint 15.9 owns duplicate/cycle/disconnected
graph rejection, `parentChain`/child-kind/provider-role coherence, termination on cycles, and mandatory
provider/kind witness coverage. The full core, Python, and demo lifecycle validation re-ran clean after the documentation/status
updates landed (2026-06-16): `cabal test all` from `core/` passes (199 tests, `DocValidatorSpec`
included), `poetry run python -m hostbootstrap.test_all` passes (139 tests) with
`poetry run python -m hostbootstrap.check_code` green, and the real Apple Silicon Lima `deploy` reported
`test report: 3/3 passed` including the `e2e-tabs` Playwright case with the topology-aware runtime
config/witness mounts (`DEMO_DEPLOY_EXIT=0`, guarded `vm down`).

### Sprint 15.7: In-place child-config delivery [Done]

**Status**: Done
**Implementation**: `core/hostbootstrap-core/src/HostBootstrap/Lift.hs`,
`core/hostbootstrap-core/src/HostBootstrap/Chain.hs`, `demo/src/HostBootstrapDemo/Commands.hs`
**Docs to update**: `documents/architecture/binary_context_config.md`,
`documents/engineering/config_generation.md`, `documents/engineering/dhall_topology.md`,
`documents/operations/demo_runbook.md`, `documents/architecture/composition_methodology.md`,
`legacy-tracking-for-deletion.md`

#### Objective

Fold child-config **delivery** into the pure lift: stream the independently produced child projection into
the child frame over the lift's `stdin` channel and have the descending binary write its own sibling
`<project>.dhall` before dispatch, replacing the build-then-copy (VM) and build-then-mount (container)
paths. The named `context-init` row remains an announcing frame anchor rather than the owner of this
effect. The payload travels on `stdin` with no parent-side intermediate file or config bind-mount, then
is persisted at the child's sibling path and is inspectable there. This sprint adjusted descriptive
context but still carried the full demo project record/resource envelope; Sprint 19.8 owns the
role-specific payload type.

#### Deliverables

- A generic `HostBootstrap.Lift` capability to carry a rendered config payload on the container handoff's
  `stdin` (a `ContainerLift` config-delivery), plus the `HostBootstrap.Chain` handoff piping that `stdin`;
  the pure argv fold stays unit-testable and the payload never appears in `argv`.
- The demo VM-config rewrite: the parent renders the narrowed VM projection and streams it into the VM
  (`streamVMConfig`), removing the host-side `demo/.build/hostbootstrap-demo.vm.dhall` and the config
  `copyFileToDemoVM` (`stageFileEffects`, used by `stageSource`, is retained).
- The demo container-config rewrite: the projection is streamed into the single `docker run … project up`
  and an entrypoint wrapper writes the sibling before exec, removing
  `hostbootstrap-demo.runtime-container.dhall` and the config bind-mount (the docker-socket and
  `/run/hostbootstrap` witness mounts are retained).
- The `/run/hostbootstrap/vm-provider` witness stays minted metal-side (the in-VM gate checks it before any
  step runs), and the VM `context-init` step stays a real frame anchor so `vm-orchestrator-1` remains in
  the chain.

#### Validation

- New pure unit tests for the container handoff argv (config on `stdin`, absent from `argv`) and the
  `stdin` extraction; the existing `LiftSpec` / `ChainSpec` `ContainerLift` fixtures updated.

#### Remaining Work

None. Closed **2026-07-02**. Static gates: `cabal build all --ghc-options=-Werror` and `cabal test all`
pass from `core/` (280 tests — `LiftSpec` adds the config-delivery cases asserting the entrypoint wrapper
argv, that the projection is **absent from `argv`**, and `liftStdin`/`configWriteScript`) and from `demo/`
(`-Werror` build + suite). Real run: a live Windows/WSL2 `test run all` reported **`test report: 6/6
passed`** across both message variants with the VM config **streamed in-place** and the container config
**streamed on the handoff `stdin`** (the entrypoint wrapper writing the sibling before dispatch) — **no**
`hostbootstrap-demo.vm.dhall` or `hostbootstrap-demo.runtime-container.dhall` produced and **no** config
bind-mount on the container `docker run` (verified: no `-v …hostbootstrap-demo.dhall` in the run argv). The
superseded build-then-copy/mount surfaces moved from `Pending` to `Removed Surfaces` in
[legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md).

### Sprint 15.8: Accelerator daemon and direct-container context [Active]

**Status**: Active
**Implementation**: `core/hostbootstrap-core/src/HostBootstrap/Context.hs`,
`core/hostbootstrap-core/test/ContextSpec.hs`, `demo/src/HostBootstrapDemo/Commands.hs`
**Docs to update**: `documents/architecture/binary_context_config.md`,
`documents/engineering/accelerator_daemon.md`, `documents/operations/demo_runbook.md`

#### Objective

Extend binary-context authority for accelerator daemon placement and the Linux GPU direct-host
project-container topology.

#### Deliverables

- Standard daemon projections that declare the accelerator service handler and worker-supervision
  classes, not project lifecycle classes; Sprint 15.9 separately prevents later unchecked widening.
- In-cluster daemon config delivery for Linux CPU/GPU daemon pods.
- Host-resident daemon config delivery for Apple Silicon and Windows GPU host daemons.
- A context/topology representation for `host -> project container -> nvkind cluster` that does not require
  a VM parent, while preserving VM ancestry checks for VM-backed runtime containers.

#### Validation

- `ContextSpec` covers daemon command authorization, project-command rejection from daemon contexts, and
  wrong-frame witness failures.
- Pure topology tests cover the direct Linux GPU project-container path, rejection of an unmarked
  host-backed project container, and the existing VM path.
- Integration tests prove all daemon placements read the expected config and connect to the web service.

#### Remaining Work

Static constructors, gates, and topology tests are complete (`deriveDaemonContext` /
`deriveHostDaemonContext` / `deriveClusterDaemonContext` and the daemon `DaemonCommand`/`ServiceCommand`
authority). The demo **materializes and delivers** every projection: the Apple/Windows post-handoff path
writes the host-resident config beside the host-native daemon binary; the Linux CPU/GPU
`deploy-accelerator-daemon` path renders the in-cluster config into a dynamically generated ConfigMap,
applies it before the workload, and sets `HOSTBOOTSTRAP_CURRENT_FRAME` to the derived `daemon-<n>` frame so
the in-pod runtime-witness gate passes. The web service config is likewise generated and applied by
`HostBootstrapDemo.Commands` from the actual parent topology; the exact mounted bytes are hashed into the
pod template so a subPath-mounted config change causes rollout. Dated validation evidence (2026-07-15):
`cabal build all --ghc-options=-Werror` and `cabal test all` passed from `core/` (364) and `demo/` (87, plus the
embedded 364-core suite).

Open only for real-run integration proving the remaining daemon placements **read** their delivered
config and **connect**: native Linux CPU/GPU in-cluster lanes plus the unavailable Apple host-daemon
lane. The dated Windows GPU/WSL2 `8/8` accepted by Phase 18 already covers the Windows host-daemon
delivery/connect path; it does not stand in for these remaining native lanes. No config implementation or
static-test work remains.

### Sprint 15.9: Opaque capability and context authority [Blocked]

**Status**: Blocked
**Blocked by**: Sprints 5.7, 9.10, and 19.7–19.8
**Implementation**: `core/hostbootstrap-core/hostbootstrap-core.cabal`,
`core/hostbootstrap-core/src/HostBootstrap/Context.hs`,
`core/hostbootstrap-core/src/HostBootstrap/Config/Class.hs`,
`core/hostbootstrap-core/src/HostBootstrap/Config/Schema.hs`,
`core/hostbootstrap-core/src/HostBootstrap/Config/Fields.hs` (introduced by Sprint 19.8),
`core/hostbootstrap-core/src/HostBootstrap/Command.hs`,
`core/hostbootstrap-core/src/HostBootstrap/Service.hs`,
`core/hostbootstrap-core/src/HostBootstrap/Lift.hs`,
`core/hostbootstrap-core/src/HostBootstrap/Chain.hs`,
`core/hostbootstrap-core/src/HostBootstrap/Step.hs`,
`demo/src/HostBootstrapDemo/Commands.hs`,
`demo/src/HostBootstrapDemo/Web/Server.hs`,
`demo/src/HostBootstrapDemo/Accelerator/Daemon.hs`,
`core/hostbootstrap-core/test/ContextSpec.hs`,
`core/hostbootstrap-core/test/CLISpec.hs`,
`core/hostbootstrap-core/test/ServiceSpec.hs` (new),
`core/hostbootstrap-core/test/Spec.hs`
**Docs to update**: `documents/architecture/binary_context_config.md`,
`documents/architecture/lifecycle_state_model.md`,
`documents/architecture/dhall_generation.md`,
`documents/architecture/hostbootstrap_core_library.md`,
`documents/engineering/config_generation.md`, `legacy-tracking-for-deletion.md`

#### Objective

Make context describe requested placement while opaque, role-specific validated capabilities grant
command authority; a caller must not be able to self-assert permission by constructing/editing context
data or by replaying a prior handoff.

#### Deliverables

- Hide capability/witness constructors and record-update escape hatches from public modules. Phase 9
  transitions mint resource/readiness authority, while a separate non-config root gate verifies installed
  project identity, OS/operator authorization, protected authority-store identity, and the exact verb
  before minting `RootInvocationAuthority scope brokerGeneration verb`; that generation must match the
  bound lease/broker, and this breaks the authority/profile/transition bootstrap cycle.
- Introduce opaque role-specific
  `CommandAuthority scope planId frame authorityEpoch verb phase`, using
  `BrokerEpoch brokerGeneration` or `BuildEpoch buildId` for project/build authority classes. Runtime
  services deliberately use the distinct inseparable `VerifiedRuntimeRoleActivation` plus
  `RoleLifecycleAdmission` and private `ServiceCommandAuthority`; they do not add a parallel generic
  controller-epoch authority. Each value also carries a hidden one-use invocation identity. Make authorization effectful: it
  atomically reserves the exact invocation record while checking the live cursor/epoch. An
  existing-frame command can open only one durable session for that exact
  plan/command/frame/epoch/phase; the operation prepare gate
  atomically advances the matching journal/session cursor. The gate
  never infers authority from decoded `BinaryContext`, declared `CommandClass`, capability list, file
  path, or Boolean.
- Replace unchecked `addRole` unioning with smart constructors that validate the primary context kind,
  added roles, command classes, capabilities, and phase together. A non-leaf primary cannot acquire
  service-run authority, and `Daemon` / `ImageBuildContainer` primaries cannot acquire
  `project up|down|destroy` authority merely by unioning `ClusterLifecycleCommand` or
  `HostOrchestratorCommand`. Every project lifecycle verb requires exact root placement: matching
  host-orchestrator primary, empty parent, verified project identity, and its verb-specific root
  authority.
- Promote decoded topology through one total graph validator before command authorization. Frame IDs are
  unique, every non-root has one resolvable parent, the graph is connected and acyclic, `parentChain`
  agrees with edges, `currentFrame` is unique/reachable, and child kind/provider/role relations are legal.
  Traversal carries a visited set and cannot diverge on a cycle. An opaque validated context constructor
  derives child frames; callers cannot assemble an unchecked graph by record update.
- Define a closed required-witness relation for each primary kind/provider/placement. Validation compares
  the exact required set with supplied evidence and verifies every member; an empty, omitted, duplicate,
  irrelevant, or contradictory witness list cannot authorize a command. Opaque projection derives that
  set jointly with the child context.
- Read and canonicalize the sibling config once, mint
  `ValidatedConfig scope specDigest configId (cfg scope)` plus its digest, and inject that exact snapshot into plan
  construction and the closed plan-operation inputs that need it. Public arbitrary `Step` callbacks do
  not survive the Sprints 9.10/16.6/19.8 target. Remove demo-global sibling reloads. The parent projects
  a non-secret role-specific descriptive wire through the jointly finalized runtime spec; the runtime
  verifies its exact mounted bytes plus the separately delivered scope-correct secret bundle and mints
  an opaque `ValidatedServiceRequest specDigest childConfigId secretDigest fields service` under a fresh
  child-local identity distinct from the parent full-config ID. Phase 18.6 internally packages it with a
  closed `ServiceProgram` handler and matching workload-instance/Serve/effect proof, then invokes the
  private `selectAndRunService` only inside `runVerifiedRuntimeRole`. Only its
  `RoleParams specDigest childConfigId secretDigest fields service` reach the handler; the full `cfg` and raw `IO` never cross that
  boundary. A file replacement
  after validation affects neither the current plan nor packaged handler parameters; a later invocation
  receives a new `configId` and must reauthorize.
- Consume Sprint 5.6.1's `CanonicalProjectRoot scope rootId` from root-config admission when opening the
  plan. Command authorization may compare the config/root binding but cannot reinterpret `sourceRoot`,
  consult `cwd`, or mint root authority from a host, guest, container, or alias pathname.
- Separate descriptive context fields from opaque local authority and narrow both across child
  projections without forwarding parent-only privileges. Parent and narrowed child bytes have distinct
  config identities; an opaque projection binding preserves scope/plan revision and child digest rather
  than reusing the parent's `configId`.
- Carry each cross-frame handoff over a private duplex session relayed to the independently authorized
  **root** broker: a length-delimited narrowed config wire plus a token bound to exact scope, plan
  revision, broker generation, parent/child edge, child config digest, verb, and phase. Immediate parents
  receive no signing/delegation key. The binary's internal receiver replaces the current shell config
  writer and returns a fresh challenge; the root signs every binding. Grant+byte verification against an
  independently installed project public key and through the scope-correct project-owned
  `ProjectCodec scope specDigest cfg` jointly mint generic
  `VerifiedConfigWire scope childConfigDigest childConfigId` and the exact `VerifiedHandoff`; raw wire
  cannot be promoted. Those values do not directly authorize dispatch:
  `withChildProjectPlan` consumes them, `ValidatedConfig`, the closed verb, and non-empty plan draft,
  verifies the stable revision, and jointly yields a fresh local `ProjectPlan`,
  `PlanDigestBinding`, and exact `ChildPlanAuthority` inside a rank-2 continuation.
  `authorizeChildProject` consumes that authority with the matching frame/cursor/context and never gives
  the child broad/delegable root, harness-root, or signing authority.
  Authority is never encoded in Dhall and neither config nor token appears in `argv` or the environment.
  Opening the child operation session additionally requires current-broker admission, advances the
  shared Open project-journal version, and returns the sole successor state/permit pair; session close
  does the same. Registering an operation's initial intent consumes the exact no-history or
  released-reacquisition generation origin, atomically adds it to that exact session, and advances both
  versions. Clean activation proves every older recorded session Closed, including
  zero-operation sessions; abandoned activation uses the exact old-permit fence set plus
  a manifest pairing the independently complete session and operation sets. Its protected interpreter
  rebinds every existing stable session record—including a zero-operation Open session—and closes every
  Open logical session while applying a private total discriminator to every recorded operation. It
  distinguishes unknown, the five pre-call continuable phases, a closed whitelist of already-observed
  retryable phases, successful, and terminal states. Only continuable phases receive current-fence
  prepare authority; only reservation/effect absence,
  ordinary or adopted same-identity teardown presence, adoption absence, repair-original, and
  managed-phase-from receive fenced same-key retry authority; successful and terminal states receive no
  effect authority. The interpreter settles the complete operation set and verifies the complete
  rehydrated resource-record set before it can admit a new session. An initial intent may have no fence
  and cannot prepare; the recovery classifier idempotently completes the stable initial-fence protocol
  and threads the sole successor session/state/permit before exposing continuable authority. Before each child effect, one
  protected prepare compare-and-swap revalidates
  the project-mode and
  broker/authority epochs, bound lease, active revision/no migration freeze, exact
  verb/phase/frame/session, Open-project state, current authoritative fence, journal version/phase,
  operation key, exact plan-owned zero/one/many precondition set, and call digest. It reruns every
  target/dependency probe and conditional version; stale/replaced/not-ready evidence returns no permit.
  It durably records the operation-specific unknown state before jointly returning the exact
  attempt-indexed `PreparedOperation` and fresh `PreparedPreconditions` required by the adapter
  together with the fresh-versioned successor Open-session, Open-project operation state, and
  revision-permit authority. The consumed journal version cannot authorize another prepare or close.
  The prepared pair retains the exact target/generation/operation/precondition-set/call-digest/session/
  fence/attempt/journal indices and must match the plan descriptor, operation binding, or teardown step;
  retained `Ready`/prerequisite values and either half are not effect authority. Every terminal
  observation returns `OperationAdvance` on success or typed failure; its eliminator yields the result
  only with the sole successor Open-project state/revision-permit pair. Initial fence creation persists/
  resumes the same proposed epoch and returns the sole successor session/state/permit; it and
  crash-time
  `FenceIntentRecorded → FenceOutcomeUnknown → FenceObserved` rotation are durable/idempotent; a delayed
  old permit is rejected or deduplicated. Terminal acknowledgment first proves every registered outcome
  settled and compare-and-swaps that session version Closed, so it cannot race another prepare.
  Recorded transcripts and replay refuse; broker loss before permit refuses, while loss afterward
  recovers by reprobe. Every later edge, including teardown, uses a fresh token and session.
- Require opaque durable-placement authority exactly where the selected program's effect row permits
  durable-state mutation and reject a merely declared but unverified `DurableStore`. The initial
  dispatch, request bundle, selection proof, and interpreter must agree; a late handler-specific reload
  is not a gate.
- Make every command gate consume its precise capability/phase token before side effects. Recursive
  lifecycle/test/teardown uses root-broker handoffs. Restartable service/daemon leaves instead require
  one `VerifiedRuntimeRoleActivation scope planDigest specDigest binaryDigest frame revision instanceId
  configDigest secretDigest service rolePlanDigest permittedEffects`. The root broker is the sole
  manifest signer after full plan validation and binds the immutable rollout revision, controller/template
  identity, and every pre-instantiation index above; it cannot sign a pod UID that does not exist yet.
  Kubernetes uses immutable digest-addressed ConfigMap, Secret, and signed-activation-manifest objects
  referenced by one pod-template revision; the Secret is the sole secret-bearing runtime object. Host
  daemons use an atomically switched revision directory/pointer. After creation, startup pairs the signed
  revision with the measured `instanceId`—pod UID plus container restart count or a protected OS-service
  invocation nonce—and hashes mounted wire/private-channel bytes. Old/new mixtures, partial publication,
  revoked keys, and cross-spec/binary replay refuse. Runtime verification uses an
  independently pinned project key and yields activation authority plus signed role projection only as
  an inseparable package with the protected secret-channel locator. It cannot mutate lifecycle state.
  Each restart verifies the non-secret role-wire bytes through the same finalized runtime spec and
  internally reads/verifies the Production-empty or Harness run-private bundle. It neither accepts a
  caller-constructed bundle input nor requires `HarnessConfigAuthority`. Cleartext fixtures may exist only
  in the Secret/private channel, never the role ConfigMap, pod template, signed activation manifest,
  envelope, logs, or diagnostic views.
  Dockerfile-time
  `check-code` requires ephemeral
  `BuildInvocationAuthority projectId specDigest configId buildId sourceDigest builderBinaryDigest` through the build engine's
  secret/session channel. The build gate uses the Production project-owned `ProjectCodec`, authenticates
  a stable config digest, binds it to the locally
  verified fresh project-indexed Production `configId`, independently measures the exact source/context
  digest, verifies the coordinator/builder binary identities from the signed grant, and jointly yields
  an `ImageBuildFrame projectId specDigest configId frame` before producing only
  `CheckCode`/`BuildPhase` authority. `ImageBuildScope` is a build command scope, not a third secret
  scope. The baked `ImageBuildContainer` config cannot authorize it,
  and the resulting image digest is recorded only after the pre-image source-bound gate succeeds.
- Supply the inseparable activation/projection package and exact-byte `RoleCodec` verification consumed
  by Sprint 14.6. The signed projection binds `specDigest`, `binaryDigest`, `rolePlanDigest`,
  `configDigest`, and `secretDigest` to the parent
  `planDigest`; a digest string in config is not proof. Sprint 14.6, not this sprint, owns
  `withRuntimeRolePlan`, `RolePlan`/`RoleCursor`, `RolePlanDigestBinding`,
  `VerifiedServicePlacement`, and the prerequisite/acquire/readiness/drain state machine. Sprint 18.6
  consumes that lifecycle foundation with the finalized registry and privately mints/transfers the
  Serve-specific service command authority. A service restart cannot construct a lifecycle
  `ProjectPlan`, generic command authority, or root authority. Provider verification must expose the
  protected compare-and-swap used by Sprint 14.6 to reserve one durable lifecycle admission before
  Prereq/acquisition; the Serve reservation is a second, narrower gate, not the first one-use check.
- Supply the independent opaque root/command authority consumed by Phase 10's lifecycle-profile openers:
  a verified Production invocation yields only Production root scope, while a verified harness root
  yields only its `Harness projectId runId` scope. Neither authority can mint a profile by itself, and
  neither is publicly forgeable through context data. Phase 10 alone combines it with the matching active
  mode and unbound lease in a fresh-profile transaction, or combines an exact Production `ProjectUp`
  root with the already-bound lease/snapshot/recovery proof in the separate bound-recovery profile
  transaction. For Production closure, this phase also
  supplies the root/verb side of the closed `ProductionClosureAuthorization`: settled destroy requires
  the exact `ProjectDestroy` root plus `DestroySettled`, while any exact Production verb can use the
  true-pre-effect constructor only with `VerifiedNoProjectResourcesAcquired`. Phase 10 owns the release
  compare-and-swap that revalidates the exact mode/lease/snapshot/Open-state tuple and complete
  Closed-session set, then atomically records `ClosedProject`, closes the invocation lease, and releases
  mode. Session opening contends on that same project-journal version, so it and finalization have one
  winner; partial `up`/`down` work cannot close and no mode-cleared partial state exists.

#### Validation

- Compile-time negative tests prove consumers cannot mint or widen capabilities.
- Compile-time and gate tests prove a non-leaf primary cannot mint service-run authority and a widened
  `Daemon` / `ImageBuildContainer` cannot mint any `project up|down|destroy` authority; valid root
  orchestrator and leaf-service constructions still dispatch.
- Compile-time negative tests prove a `TestComponent` cannot obtain Production profile authority.
- Projection/gate tests cover privilege narrowing, identity mismatch, missing witness, direct-container
  placement, durable-state denial, and valid multi-role contexts.
- Property tests cover duplicate IDs, cycles (with bounded termination), disconnected/orphan frames,
  `parentChain` disagreement, illegal child kinds/providers/roles, missing/duplicate/extra/false required
  witnesses, and valid direct/VM/service/daemon placements. Only total validated contexts reach
  authorization.
- TOCTOU tests replace the sibling file after initial validation and prove plan steps/child projections
  retain the original parent `configId`/digest/value while handler execution receives only parameters
  packaged under the fresh, different child-local `configId` produced by exact role-wire verification.
  No production action reopens the sibling path; a second invocation validates a new snapshot
  independently.
- Controller-restart tests verify unchanged mounted bytes can mint a fresh local validated Production or
  Harness request for the exact signed revision and measured instance, while a changed ConfigMap/Secret
  hash, another project/run, stale revision, reused pod UID with a different restart count, and stale OS
  invocation refuse. A Harness restart with fixture bytes succeeds only through the activation-bound
  private-channel verifier, never a replayed `ConfigHandoff` or `HarnessConfigAuthority`.
- Transport tests prove length framing, exact child/config-hash binding, fresh-challenge authentication,
  single consumption/replay and recorded-transcript refusal, rejection of broker loss, truncated or
  mismatched envelopes, a verification key supplied by the envelope, raw-wire promotion, fresh teardown
  tokens, exact `BoundRunLease scope specDigest planDigest brokerGeneration` nonce consumption, and absence of
  authority from Dhall, `argv`, and environment. Prepare/session tests prove exact authority
  epoch/verb/phase/Open-project state/current fence, exact precondition set/fresh prepared conditions,
  and the durable unknown phase predate every effect;
  retained `Ready`, either prepared half, a wrong edge/digest/version, target/operation binding, or
  teardown step cannot call the adapter; both success and typed
  failure return the sole successor state/permit pair through `OperationAdvance`; one invocation cannot
  open two sessions, and terminal/lost acknowledgment is exact-version closure only. Kill/race tests
  cover initial fence creation, every fence-rotation phase, delayed old permits, session
  open/prepare/outcome/settle/close, and prepare versus project close. Kill after every continuable
  pre-call phase and persisted retryable observation proves only the matching current-fence or closed
  fenced same-key branch can resume; successful and terminal branches cannot receive effect authority,
  and intent/session membership cannot split. After a kill immediately after that atomic intent write but
  before the first fence record, recovery starts and persists the sole epoch; only a later interruption
  resumes that persisted epoch. Neither path can prepare early. Missing/
  duplicate session, operation, or rehydrated-resource
  members yield no current-broker admission. Normal and completed-migration activation both settle prior
  sessions and produce the new revision's admission before any new session can open.
  Production finalizer races prove session open and the atomic
  `ClosedProject`/closed-lease/released-mode transition have exactly one winner; kill/restart observes no
  partial release.
- Type/gate tests reject the root-authority bootstrap cycle, wrong verb/phase/plan replay, and authority
  reuse without the journal transition.
- Positive build fixtures jointly construct the Production-validated config, matching
  project/config-indexed image-build frame, measured source context, and check authority. Same-role
  frame substitution from another config/project, wrong role/frame, wrong source, and an attempted
  Harness/build-config scope fail before dispatch.
- Runtime/build tests prove controller restart identity can mint only leaf activation, wrong
  revision/instance/spec/binary/config/secret/role-plan bindings refuse, and retained R1/I1 values cannot
  run in R2/I2 or after exact I1 terminates. Non-exclusive rollout overlap remains valid until controller
  shutdown. Exclusive/mutating transfer withholds the successor lease until an atomic backend fence or
  retained-lock in-flight-settlement barrier excludes old mutation; unsupported backends refuse, and
  every later R1/I1 prepare then fails. A duplicated activation/
  request cannot open two lifecycle admissions or acquire resources twice.
  Baked config alone cannot run build checks, and a
  build authority rejects wrong project/config/build/source identities. Backends without these
  identity/epoch channels return `Unsupported`.
- Live recursive runs prove a child can use only its projected authority and requests a grandchild grant
  through the root relay rather than signing it.
- Recovery projection tests prove a recovered frame and closed owned-or-released evidence arise only from
  the exact bound snapshot, plan binding, complete rehydrated resource set, and closed forest-produced
  teardown point/step. Only owned evidence contains a managed handle/receipt; only released evidence
  contains a verified tombstone, and fresh-generation rollover additionally requires protected absence,
  the sole reacquisition-origin consumer, and atomic registration. Stale/reused/wrong-resource rollover
  tokens fail the compare-and-swap; first acquisition requires no-history evidence.
  Production closure fixtures prove the settled constructor is destroy-only and the any-verb constructor
  is true-pre-effect-only.

#### Remaining Work

Blocked until Sprints 5.7, 9.10, and 19.7–19.8 land the durable-state
proof, ownership/result types,
scoped codec, and finalized plan. Then refactor context/capability construction around the Phase 9 opaque
tokens, replace the current class-membership-only project-up gate and raw `addRole`, replace the shell writer with the framed
broker/challenge/grant `BoundRunLease`/`HandoffToken` transport plus the versioned session/fence prepare
protocol, enforce every gate, and update schema/golden
tests. Coordinate
lease/token acquisition, consumption, and unwind with
[Sprint 10.9](phase-10-standardized-test-harness.md) and
[Sprint 16.6](phase-16-project-lifecycle-command.md), which own exclusive run ownership and recursive
lifecycle state. Sprint 15.8 remains Active only for native accelerator-lane validation.

## Documentation Requirements

**Architecture docs to create/update:**
- `documents/architecture/binary_context_config.md` - canonical local-config context contract and
  command-gating rules.
- `documents/architecture/python_haskell_boundary.md` - Python does not read or write Dhall.
- `documents/architecture/build_and_run_model.md` - host-native build followed by project-binary config
  initialization when needed.
- `documents/architecture/composition_methodology.md` - self-reference lift plus explicit local context.
- `documents/architecture/durable_state.md` - what the `.data` invariant does and does not guarantee, and
  why the current partial, late, self-asserted `DurableStore` check is not enforcement.
- `documents/engineering/accelerator_daemon.md` - daemon context authority and direct-container topology.

**Engineering docs to create/update:**
- `documents/engineering/config_generation.md` - narrowed config generation versus the separate
  `UnboundRunLease` → verified-snapshot-bound `BoundRunLease`/`HandoffToken` handoff and atomic nonce
  consumption.
- `documents/engineering/dhall_topology.md` - local runtime config, generated child configs, and generated
  Dhall roles.
- `documents/engineering/schema.md` - project-local `<project>.dhall` shape.
- `documents/engineering/resource_budgeting.md` - budget flows through local config envelopes and child
  projections.
- `documents/engineering/derived_dockerfile.md` - project-owned image-build config materialization through
  the `project init` family before normal commands.
- `documents/engineering/derived_project_standards.md` - derived projects materialize runtime contexts.
- `documents/operations/demo_runbook.md` - host, VM, VM-container, and cluster-service
  `hostbootstrap-demo.dhall` configs.

**Cross-references to add:**
- `README.md`, `documents/README.md`, `DEVELOPMENT_PLAN/README.md`, `00-overview.md`,
  `system-components.md`, and `development_plan_standards.md` name Phase 15 and link to the context
  contract.
