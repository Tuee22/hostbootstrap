-- The full best-effort warm-store build. The two packages are organizational
-- dependency groups built into one shared Cabal store; they produce no consumer
-- freeze or version contract. See documents/engineering/warm_store.md.
import: warm-store.config

packages:
  core/
  daemon/
