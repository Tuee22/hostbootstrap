module ImportConfigClassInternal where

-- A downstream consumer cannot import the hidden owner of the installed
-- project codec, its constructor, or its specification reindex kernel.
import HostBootstrap.Config.Class.Internal
