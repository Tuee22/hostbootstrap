module OpenObservedBoundInvocationRecovery where

-- The scope-only durable observation is package-private Harness-classification
-- machinery; public callers cannot name it as recovery authority.
import HostBootstrap.Lifecycle.Mode (ObservedBoundInvocationRecovery)
