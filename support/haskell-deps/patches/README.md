# Haskell Dependency Patches

Put temporary dependency patches here only when upstream Hackage bounds or
releases cannot build with GHC 9.14.1. Prefer small checked-in patches over
external source overrides, and remove patches once upstream releases catch up.
